# 股票分析工作流风险分析方法缺陷审计报告

> 审查范围：stock-analysis 工作流中风险分析的全链路方法论、代码实现与流程设计
> 审查日期：2026-06-28
> 审查人：助理

## 执行摘要

本次审查覆盖风险分析的全栈链路——从三个风险偏好 LLM Agent（risk-agg/risk-con/risk-neu）的 prompt 设计、聚合器（agg-risk）、风险收敛（risk-convergence）、风险等级分类器（cls-risk-level）、确定性决策引擎（portfolio-mgr.rhai），到前端风险雷达图（RiskMatrix.tsx）。

**发现 2 个 CRITICAL 级缺陷、4 个 HIGH 级缺陷、3 个 MEDIUM 级缺陷、2 个 LOW 级缺陷。**

---

## 风险分析架构总览

```
上游数据 (分析师/辩论/算法)
    │
    ▼
┌─ p-risk-assess (ParallelNode, 三档并行) ─────────────────┐
│  risk-agg (激进派)    risk-con (保守派)    risk-neu (中性派) │
│  各输出 VERDICT JSON (stance, position_pct, confidence)   │
└────────────────────────┬──────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────┐
│  agg-risk (AggregatorNode, strategy="all")                 │
│  输出 3 个结果的 JSON 数组                                   │
└────────────────────────┬──────────────────────────────────┘
                         │
                         ▼
┌────────────────────────────────────────────────────────────┐
│  risk-convergence (AgentNode)                              │
│  输出 VERDICT (converged_risk_level, converged_position_pct)│
└────────────────────────┬──────────────────────────────────┘
                         │
t-scoring ──┐            │           t-valuation ──┐
             ▼            ▼                        ▼
         ┌──────────────────────────────────────────────┐
         │  t-risk (compute_portfolio_risk)              │ ← ⚠️ 组合级别工具,单股无意义
         └──────────────────────┬───────────────────────┘
                                ▼
         ┌──────────────────────────────────────────────┐
         │  cls-risk-level (LlmClassifierNode)           │
         │  输出: risk-level.category = "低/中/高/极高"   │
         └──────────────────────┬───────────────────────┘
                                ▼
         ┌──────────────────────────────────────────────┐
         │  portfolio-mgr (CodeNode + Rhai)              │
         │  f4_signal → 贝叶斯方向因子                    │
         │  risk_bias → action 阈值偏移                   │
         │  Kelly 仓位上限 → 风险等级对应上限              │
         │  V43 硬性否决 → action 强制降级                │
         │  V47 空头否决 → 空头强制卖出                   │
         └──────────────────────────────────────────────┘
```

---

## CRITICAL 级缺陷

### C1: 三个风险 Agent prompt 中 stance 输出示例全部复制粘贴错误

**严重性**: CRITICAL — 直接影响 LLM 输出字段准确性

**位置**:

- `src-tauri/agency_experts/stock-analysis/conservative-debator.md:93`
- `src-tauri/agency_experts/stock-analysis/neutral-debator.md:76`

**问题**：

三个风险评估 Agent 的 prompt 输出格式示例完全一致地写为 `"stance": "aggressive"`：

**激进派**（正确）：

```
<!-- VERDICT: {"stance": "aggressive", "position_pct": 50, "confidence": 70} -->
```

**保守派**（错误）：

```
<!-- VERDICT: {"stance": "aggressive", "position_pct": 50, "confidence": 70} -->
```

↑ **应为 `"stance": "conservative"`**

**中性派**（错误）：

```
<!-- VERDICT: {"stance": "aggressive", "position_pct": 50, "confidence": 70} -->
```

↑ **应为 `"stance": "neutral"`**

**影响**：

LLM 在 few-shot 学习下会严格遵循输出格式示例。保守派和中性派 LLM 在 prompt 的「输出格式」章节读到的都是 `"aggressive"`，极大概率照抄该值，导致：

- `risk-con` 输出 `stance="aggressive"`（实际为保守派）
- `risk-neu` 输出 `stance="aggressive"`（实际为中性派）
- 下游解析 `stance` 字段时无法区分三方立场

**根因**：从 aggressive-debator.md 复制粘贴创建其他两个文件时忘记修改示例值。

**修复**：改不动 prompt 示例值而是写后面的 stance 字段说明太弱，LLM 会优先按示例输出。必须改示例：

```diff
  <!-- VERDICT: {"stance": "aggressive", "position_pct": 50, "confidence": 70} -->
+ <!-- VERDICT: {"stance": "conservative", "position_pct": 20, "confidence": 60} -->
```

```diff
  <!-- VERDICT: {"stance": "aggressive", "position_pct": 50, "confidence": 70} -->
+ <!-- VERDICT: {"stance": "neutral", "position_pct": 35, "confidence": 65} -->
```

---

### C2: `compute_portfolio_risk`（t-risk）是组合级别工具，对单股分析输出无意义

**严重性**: CRITICAL — 单股分析的核心风险源输出语义无效

**位置**:

- `src-tauri/src/commands/stock_analysis_setup.rs:2016`
- `src-tauri/crates/tools/src/tools/stock_data.rs:1443-1608`

**问题**：

`compute_portfolio_risk` 工具的 input schema 要求 `stock_codes`（逗号分隔的多个股票代码），输出的是**组合级别**的风险指标：

```json
{
  "concentration": { "hhi": 1.0, "effectiveN": 1.0, "label": "高度集中" },
  "diversification": { "label": "集中风险" },
  "sectorExposure": [{ "sector": "某行业", "weight": 1.0 }],
  "maxSectorConcentration": { "weightPct": 100.0 }
}
```

当只传入一只股票时：

- **HHI = 1.0**（100% 集中，对于单股天然成立）
- **Effective N = 1.0**（仅1只有效股票）
- **分散度 = "集中风险"**（单股必然集中）
- **行业暴露 = 100%**（单股必属某行业）

**这些指标对于单股风险评估毫无信息量**——它们描述的是组合的分散程度，而非单只股票的风险特征。

**更严重的是**，`cls-risk-level`（LlmClassifier）的 `input_var` 设为 `"t-risk"`，其 prompt 要求 LLM 从多维度判断风险等级，但唯一的结构化输入是这个组合维度的输出，完全不包含：

- 波动率 / 贝塔系数
- VaR / CVaR
- 最大回撤
- 风险调整后收益（夏普比率）
- 历史价格风险

**影响**：

1. `compute_portfolio_risk` 对单股分析的价值接近于零
2. `cls-risk-level` LLM 只能从 `t-risk` 的冗余文本输出（HHI=1.0/集中风险）和无用的分散度指标中猜测风险等级
3. 整个"算法风险计算→LLM 风险分类"链路实际上没有一个真正的单股风险量化指标

**注意**：单股风险指标如最大回撤、夏普比率、VaR、凯利公式等**已有实现**（`risk.rs`），但没有集成到 `t-risk` 链路中。它们仅在 LLM Agent 层的 tool 调用中按需使用。

**修复方案**：

**方案 A（推荐）**：在 `risk.rs` 中新增 `compute_single_stock_risk()` 函数，整合已有纯函数（波动率、VaR、最大回撤、夏普比率），输出真正的单股风险画像。`t-risk` 工具按输入参数分支：`stock_codes` 有多个→组合风险；`stock_code` 单个→个股风险画像。

**方案 B（快速修复）**：将 `cls-risk-level` 的 `input_var` 改为直接引用含真实风险数据的上游节点（如 `t-scoring` 含技术评分和波动率信息），或至少同时传入多个数据源。

**方案 C（替代链路）**：将 `cls-risk-level` 的 prompt 改为直接从 `risk-convergence` 的分析报告中提取风险等级，而非依赖无意义的组合风险指标。

---

## HIGH 级缺陷

### H1: `cls-risk-level` 依赖无意义数据做风险分类，缺乏结构化风险指标

**严重性**: HIGH — 风险等级分类基础薄弱

**位置**: `src-tauri/src/commands/stock_analysis_setup.rs:2101-2130`

**问题**：

`cls-risk-level` LlmClassifierNode 的配置：

```rust
input_var: "t-risk".into(),
```

这意味着 LLM classifier 唯一能看到的是 `compute_portfolio_risk` 的输出（HHI/分散度/行业暴露等组合指标）。其 prompt 要求从"技术面风险、估值风险、流动性风险、基本面风险"四个维度判断，但：

1. **技术面风险** → 不在 t-risk 输出中
2. **估值风险** → 不在 t-risk 输出中（在 t-valuation 中，但未传递）
3. **流动性风险** → 不在 t-risk 输出中
4. **基本面风险** → 不在 t-risk 输出中

`cls-risk-level` 的 `context_sources` 也未设置，LLM 无法从其他节点获取数据。

**影响**：LLM classifier 在几乎没有可用信息的情况下做风险分类，本质上是基于 t-risk 的"集中风险"标签进行猜测，生成的 risk-level 可信度存疑。

---

### H2: 风险收敛（risk-convergence）的输出未被 portfolio-mgr 使用

**严重性**: HIGH — 复杂的三方风险收敛计算后结果被丢弃

**位置**:

- `stock_analysis_setup.rs:1986-2006`（risk-convergence 创建）
- `portfolio-mgr.rhai`（决策引擎）
- `stock_analysis_setup.rs:2408-2435`（portfolio-mgr input_mapping）

**问题**：

`risk-convergence` 节点是**昂贵的 LLM 调用**（约 5-10s），它执行：

1. 读取三方风险评估报告
2. 分析共识点与分歧点
3. 输出 `converged_risk_level`、`converged_position_pct`、`disagreement_score`

但查看 `portfolio-mgr` 的 `input_mapping`：

```rust
input_mapping: [
    ("overall_risk", "risk-level.category"),          // 来自 cls-risk-level
    // ...
    ("consensusScore", "debate-convergence.content.consensus_score"),  // 来自辩论收敛
]
```

**`risk-convergence` 的输出字段没有一个被注入到 portfolio-mgr 的决策公式中！**

- `risk-convergence.content.converged_risk_level` — 未使用
- `risk-convergence.content.converged_position_pct` — 未使用
- `risk-convergence.content.disagreement_score` — 未使用

整个 LLM 调用的输出被完全旁路，portfolio-mgr 最终只使用了 `cls-risk-level` 的 4 档分类结果。

**根因**：`cls-risk-level` 被添加后取代了 `risk-convergence` 的风险等级输出功能，但 `risk-convergence` 节点本身未被移除或重构。形成了"生成→丢弃"的死代码链路。

**修复建议**：

方案 A：将 `risk-convergence.content.disagreement_score` 加入 portfolio-mgr input_mapping，使分歧度作为独立的贝叶斯因子（f7）参与融合。

方案 B：移除 `risk-convergence` 节点（或改为轻量级纯文本聚合），节省每次分析的 5-10s LLM 延迟。

---

### H3: `cls-risk-level` LlmClassifier 的 prompt 缺少实际数据源引用

**严重性**: HIGH — LLM 需要在无数据的情况下凭空判断

**位置**: `stock_analysis_setup.rs:2110-2122`

**问题**：

Classifer prompt 文本写死为：

```
"根据技术评分、估值计算和风险评估的输出结果，
 从多维度判断该股票的整体风险等级。

 评估维度：
 1. 技术面风险：趋势是否逆转？RSI是否超买？成交量异常？
 2. 估值风险：PE/PB处于历史高位？是否严重高估？
 ...
```

该 prompt 是**静态字符串**，没有通过模板变量注入任何实际数据。唯一能进入 LLM 上下文的是 `input_var: "t-risk"` 的 raw 输出。但 LLM 从组合风险工具的输出（HHI/EffectiveN/行业暴露）中提取"技术面风险"或"估值风险"是无从谈起的。

**对比**：其他 Agent 节点的 prompt 都有 `{{stock_code}}` 和 `{{stock_name}}` 模板变量，以及通过 `context_sources` 注入的大量上下文。`cls-risk-level` 没有这些。

**影响**：LLM classifier 在零可用数据的情况下，只能从 `t-risk` 的组合指标中强行推断风险等级，分类质量极低。

---

### H4: f4_signal 映射过于粗糙，敏感度不足

**严重性**: HIGH — 风险因子的贝叶斯信号强度非线性跳跃

**位置**: `portfolio-mgr.rhai:81-86`

**问题**：

```rhai
let f4_signal = if f4_weight > 0.0 {
    switch overall_risk {
        "低风险"|"低" => 0.5, "中" => 0.0,
        "高风险"|"高" => -0.4, "极高" => -0.7, _ => 0.0
    }
} else { 0.0 };
```

映射映射使用离散跳变：

| 风险等级 | f4_signal | 邻跳 | 语义突变      |
| -------- | --------- | ---- | ------------- |
| 低风险   | +0.5      | —    | 大幅加成      |
| 中风险   | 0.0       | 0.5  | **骤降 50pp** |
| 高风险   | -0.4      | 0.4  | 跳入负区      |
| 极高风险 | -0.7      | 0.3  | 进一步负向    |

**问题 1：中风险→0.0 是不对称的**
中风险没有任何信号贡献，即"中性→不贡献任何信息"。但更合理的处理应该是"中风险→轻微负信号"（如 -0.15），因为中风险并不意味着风险为零。

**问题 2：低风险→+0.5 和极高风险→-0.7 不对称**
低风险的信号强度（+0.5）远低于极高风险的强度（-0.7），且两者绝对值不匹配。如果极端看空的风险响应强度高于极端看多，会在低 beta 市场中系统性地偏向保守。

**问题 3：风险等级之间的跳跃非线性**
从低风险(+0.5)到中风险(0.0)的降幅是 0.5，而从高风险(-0.4)到极高风险(-0.7)的降幅是 0.3。这意味着"由低转中"的风险警告强度高于"由高转极高"，逻辑不自洽。

**修复建议**：

```rhai
let f4_signal = if f4_weight > 0.0 {
    switch overall_risk {
        "低风险"|"低" => 0.35,    // 降低正向偏离
        "中风险"|"中" => -0.10,   // 中风险有轻微负信号
        "高风险"|"高" => -0.40,   // 保持
        "极高风险"|"极高" => -0.65, // 略降低
        _ => 0.0
    }
} else { 0.0 };
```

或改为连续映射：`(max(低=80, 中=50, 高=20, 极高=0) → 线性映射到 [-0.7, +0.5])`。

---

## MEDIUM 级缺陷

### M1: `risk-convergence` 的下界约束风险规则与 neutral 派核心逻辑冲突

**严重性**: MEDIUM — 两个风险收敛逻辑相互矛盾

**位置**: `risk-convergence.md:66`

**问题**：

risk-convergence prompt 的第三轮规则规定：

```
4. 仓位上下界：最终仓位不应低于保守派建议的80%，也不应高于激进派建议的120%
```

但 `neutral-debator.md` 的核心逻辑是**共识度折回**——当三方分歧大时，中性派应大幅折回（最低到中位值的 50%）：

```
else:                              # 弱共识 / 严重分歧
    positionPct = round(median_pct * 0.5)
```

**矛盾**：如果保守派给 10%（很低），激进派给 80%（很高），中位可能是 35%。中性派按共识度折回到 17.5%。但风险收敛分析师却受规则约束，不能输出低于保守派 80%（8%）的值。由于 17.5% > 8%，这个约束在多数情况下不起作用，但规则的设计意图（"取保守方为锚"）与中性派的"分歧越大越保守"逻辑方向一致但实现不同。

**真正的问题**：风险收敛下界规则约束 `converged_position_pct`，但这个值**没有被 portfolio-mgr 使用**（见 H2），所以该规则实际无效——但它会误导阅读代码的人认为风险收敛的输出会影响最终决策。

---

### M2: 风险 Agent 缺少 `context_sources` 的 `risk-level` 数据

**严重性**: MEDIUM — 三档风险评估 LLM 不知道 `cls-risk-level` 的分类结果

**位置**: `stock_analysis_setup.rs:1920-1936`

**问题**：

三个风险评估 Agent（risk-agg/risk-con/risk-neu）的 `context_sources` 包含了 9 个分析师、辩论结果和技术指标，但**没有 `risk-level` 的节点 ID**。这本身是合理的——因为风险分类器 `cls-risk-level` 在风险评估 Agent 之后执行。

但这意味着：三档风险评估 Agent **不知道整个系统对风险等级的分类**，它们只能靠自己的 LLM 判断。而 `cls-risk-level` 在它们之后，依赖的是 `t-risk`（组合风险工具）的无效数据（见 C2）。

这个时序问题意味着：

1. 评估 Agent 的输出 → 不被用于最终决策的 risk level（由 cls-risk-level 决定）
2. cls-risk-level 的输出来自无意义的数据源（t-risk 的组合指标）

**两类风险判断（Agent 的主观判断 vs 分类器的客观分类）各来自不可靠的数据源**。

---

### M3: 前端 `computeRiskScore` 的 `position_pct` 反转规则与语义不匹配

**严重性**: MEDIUM — 风险雷达图评分与语义有偏差

**位置**: `src/components/stock-analysis/RiskMatrix.tsx:285-288`

**问题**：

```typescript
// 激进派给高仓位(70)→风险分低(30)；保守派给低仓位(20)→风险分高(80)
const posPct = extractVerdictField(text, "position_pct");
if (posPct !== null && posPct >= 0 && posPct <= 100) {
  return Math.max(5, Math.min(100, 100 - posPct));
}
```

该逻辑假设：**仓位越低=风险越高**。这对保守派成立（保守派给低仓是因为看到了风险），但对激进派**不成立**。

激进派的 `position_pct` 是"收益最大化的仓位建议"，与激进派对风险的评估无关。激进派 prompt 中的 Kelly 公式逻辑决定了：当 Kelly 为负时 stance 变为 neutral 且仓位=0，否则正仓位。

因此：

- 激进派给 70% 仓位 → 风险分 30（低估了激进派实际感知的风险）
- 激进派给 0% 仓位 → 风险分 100（Kelly 为负，但风险分被抹平为 100）

更好的做法是使用 `confidence`（第 2 优先级）而非 `position_pct` 作为风险度量，因为 `confidence` 体现了评估师对自身判断的确定性程度。

---

## LOW 级缺陷

### L1: `aggressive-debator.md` 的 stance 示例与自检冲突

**位置**: `aggressive-debator.md:70-80`

**问题**：

自检第一条：

```
- [ ] position_pct 是否有充分的风险依据？
```

但激进派的核心是"收益最大化"，框架中列出的是 A 股动量因子（涨停动量、政策底、PE 扩张、散户放大、游资确认），**没有一项是风险因子**。自检要求"风险依据"但 prompt 没有给出风险评估框架。

这是一种框架缺失：激进派没有定义自己如何评估风险（只有如何评估收益），导致 LLM 输出的"风险评估"实质上是"收益评估"。

---

### L2: `risk.rs` 中有完整单股风险函数但未被集成到工作流

**位置**: `src-tauri/crates/stock-analysis/src/risk.rs`

**问题**：

`risk.rs` 包含 7 个纯函数风险模型，其中 5 个可直接用于单股风险量化：

| 函数                                           | 能力       | 适用单股 |
| ---------------------------------------------- | ---------- | -------- |
| `max_drawdown(prices)`                         | 最大回撤   | ✓        |
| `sharpe_ratio(returns, rf)`                    | 夏普比率   | ✓        |
| `value_at_risk(returns, conf)`                 | VaR / CVaR | ✓        |
| `pe_percentile(pe, hist)`                      | PE 分位数  | ✓        |
| `kelly_criterion(win_rate, avg_win, avg_loss)` | 凯利公式   | ✓        |

这些函数都是通过 ToolDef 注册到 LLM Agent 层的，**但不被 `t-risk` 工具或 `cls-risk-level` 分类器使用**。LLM Agent（风险评估师）可以自主决定是否调用这些工具，但 LLM 的调用行为不可控——部分评估师可能不调用任何工具就输出结论。

**建议**：在 `compute_portfolio_risk` 工具中增加单股分支，当输入为单只股票时，自动调用这些纯函数生成真正的单股风险画像。

---

## 风险分析链路数据流示意图

```
当前"有效"数据流（粗线=有效，细线=薄弱，虚线=无效）：

上游分析师 ──[context_sources]──► risk-agg ──► VERDICT {stance, position_pct, confidence}
上游分析师 ──[context_sources]──► risk-con ──► VERDICT {stance, position_pct, confidence}
上游分析师 ──[context_sources]──► risk-neu ──► VERDICT {stance, position_pct, confidence}
                                           │
                                           ├─[agg-risk]──► [数组] ──► risk-convergence
                                           │                              │
                                           │                   输出(未使用) │
                                           │                              ▼
                                           │                     ┌── 死代码 ──┐
                                           │                     │ 被丢弃    │
                                           │                     └──────────┘
t-scoring ──► t-valuation ──► t-risk(组合) ──► cls-risk-level ──► "低/中/高/极高"
                                  ↑                              │
                           组合指标对单股无意义                      │
                                                                   ▼
                                                          portfolio-mgr
                                                          f4_signal ← overall_risk
                                                          risk_bias ← overall_risk
                                                          Kelly cap ← overall_risk
                                                          V43 veto ← overall_risk
```

---

## 修复优先级建议

| 优先级 | 编号        | 描述                                                      | 工作量 | 风险 |
| ------ | ----------- | --------------------------------------------------------- | ------ | ---- |
| **P0** | C1          | 修复保守派和中性派 prompt stance 示例                     | 2 行   | 极低 |
| **P0** | C2          | 重构 `compute_portfolio_risk` 支持单股风险画像            | 2-3 天 | 中   |
| **P1** | H1          | 为 `cls-risk-level` 增加 context_sources 或换 input_var   | 半天   | 低   |
| **P1** | H2          | 将 `risk-convergence` 输出接入 portfolio-mgr 或移除该节点 | 1 天   | 中   |
| **P1** | H3          | 为 `cls-risk-level` prompt 增加真实数据模板变量           | 半天   | 低   |
| **P2** | H4          | 优化 f4_signal 映射，增加中风险负信号                     | 数行   | 低   |
| **P2** | M3          | 前端风险雷达图改用 confidence 而非 position_pct           | 数行   | 低   |
| **P3** | M1/M2/L1/L2 | 文档清理、框架补充                                        | 可推迟 | 极低 |

---

## 关键结论

1. **风险分类的根基薄弱**：`cls-risk-level` 依赖的 `t-risk` 工具输出组合级别指标（HHI/分散度），对单股分析无信息量。单股风险函数（`risk.rs` 中的 max_drawdown/VaR/Sharpe 等）已存在但未被集成到自动分类链路中。

2. **昂贵 LLM 调用被浪费**：`risk-convergence` Agent 是每次分析的重大成本（5-10s LLM + 推理），但其结构化输出（`converged_risk_level`、`converged_position_pct`、`disagreement_score`）无一被 portfolio-mgr 使用，形成"生成→丢弃"的死代码。

3. **Prompt 复制粘贴缺陷**：保守派和中性派的输出格式示例错误地写为 `stace: "aggressive"`，导致三个风险评估 Agent 输出难以区分。

4. **f4_signal 映射粗糙**：离散跳跃的 4 级映射敏感度不均，中风险被映射为 0.0（无贡献），与"中风险≠无风险"的常识矛盾。

5. **后端有全套单股风险量化能力**：`risk.rs`/`portfolio_monitor.rs` 已实现最大回撤、夏普比率、VaR、PE 分位数、PEG、凯利公式、集中度、相关性等纯函数，问题在于这些能力没有被有机地串联到自动化的风险分类链路中。
