# 瓶颈掘金工作流（Serenity-Screening）缺陷审计报告

> 审计日期：2026-06-27 | 审计人：助理 | 版本：V52
> 第二次复查：2026-06-27 20:54

---

## 执行摘要

第一次审计发现 **9 个缺陷**（2 CRITICAL / 3 HIGH / 2 MEDIUM / 2 LOW），**5 个已修复**，**4 个确认为非缺陷**。

第二次复查确认所有之前修复正确，同时发现 **4 个新缺陷**（均为 MEDIUM/LOW 级）：

| 新编号  | 级别   | 位置                           | 描述                                                                       |
| ------- | ------ | ------------------------------ | -------------------------------------------------------------------------- |
| **D10** | MEDIUM | `stock_analysis_setup.rs:5803` | candidate-mapper prompt 引用 `demand_validation` 但不在 context_sources 中 |
| **D11** | LOW    | `stock_workflow.rs:4102`       | 同一候选股来自多个瓶颈趋势时产生重复 reco_picks                            |
| **D12** | LOW    | `stock_workflow.rs:4014`       | frontend event 在持久化之前发送，DB 失败时数据显示不一致                   |
| **D13** | LOW    | `stock_analysis_setup.rs:5558` | 7 个基线导致 x 轴布局溢出（x=1240）                                        |

---

## 第一次审计（回顾）

### 已修复 (5/9)

| 编号 | 级别     | 缺陷                                               | 修复验证                                               |
| ---- | -------- | -------------------------------------------------- | ------------------------------------------------------ |
| C1   | CRITICAL | StorageNode 集合名与实际存储表不匹配               | ✅ 已删除 StorageNode 块，注释说明 Rust 代码处理持久化 |
| C2   | CRITICAL | chokepoint-identifier 缺少 chain-decomposer 直接边 | ✅ 增加 `e-{decomposer_id}-{chokepoint_id}` 边         |
| H1   | HIGH     | trend-scanner.md 仍引用已移除的 get_hot_stocks()   | ✅ 替换为说明注释                                      |
| M2   | MEDIUM   | 仅 3 个行业基线 → 扩展到 7 个                      | ✅ 代码已更新，Rhai 同步更新                           |
| L2   | LOW      | exit_now 候选无自动剔除                            | ✅ 增加过滤逻辑 + 持久化使用 filtered candidate_array  |

### 确认为非缺陷 (4/9)

| 编号 | 理由                                                           |
| ---- | -------------------------------------------------------------- |
| H2   | decomposer 通过 `#{i}` + 文本注入区分趋势，prompt 已处理空趋势 |
| H3   | .md 文件与运行时 prompt 用途不同，两级架构设计                 |
| M1   | Rhai `!= ()` 是标准 null 检查模式                              |
| L1   | 动态节点需引擎层支持                                           |

---

## 第二次复查 - 新发现的缺陷

### D10 (MEDIUM): Candidate-mapper prompt 引用 context 中不存在的数据

**位置**：`stock_analysis_setup.rs:5803`

```rust
8. **需求确定性验证**：检查上级节点提供的 demand_validation/demand_evidence，\
```

**问题**：candidate-mapper 的 context_sources 只包含 3 个 chokepoint-identifier 节点。chokepoint 的输出结构为：

```json
{"trend_name": "...", "if_data_gaps": bool, "verified_bottleneck": {...}, "summary": "..."}
```

其中 `verified_bottleneck` 包含综合评分和顶瓶颈环节，但**不包含**原始产业链拆解的 `demand_validation` 数据（那是 chain-decomposer → CodeNode 产生的）。

candidate-mapper 的 prompt 告诉 LLM"检查上级节点提供的 demand_validation/demand_evidence"，但这些数据根本不在 context 中。

**影响**：

- LLM 可能编造不存在的 `demand_validation` 数据（幻觉）
- LLM 可能跳过此检查（但 prompt 强调"无硬证据扣 20 分"）
- 候选股的 demand_certainty 评分不可靠

**修复**：

- 方案 A：将 trend-scanner 和 chain-decomposer 加入 candidate-mapper 的 context_sources（增加边）
- 方案 B：修改 prompt，删除"检查上级节点提供的 demand_validation/..."，改为"使用 search_news 独立验证需求"
- 推荐方案：方案 A + B 结合——增加 context_sources 同时优化 prompt

---

### D11 (LOW): 候选股去重缺失

**位置**：`stock_workflow.rs:4102-4118`

**问题**：两个不同的瓶颈趋势可能推荐同一只候选股（例如 "002371" 在 trend1 和 trend2 中都出现）。`run_serenity_screening` 的持久化循环没有去重逻辑：

```rust
let pick_id = format!("serenity-{ts_ms}-{i}-{code}");
// 没有检查 reco_picks 中是否已存在同一 stock_code + generated_at
```

**影响**：

- 前端候选列表可能出现重复股票
- 用户看到同一股票两次，造成困惑
- SerenityStrategy 可能生成重复的推荐

**修复**：在持久化循环中添加 `stock_code` 去重，只保留置信度最高的那次：

```rust
let mut seen_codes = std::collections::HashSet::new();
for c in &candidate_list {
    let code = c["stock_code"].as_str().unwrap_or("");
    if code.is_empty() || !seen_codes.insert(code.to_string()) {
        continue; // 跳过已处理的 code
    }
    // ... 持久化逻辑
}
```

---

### D12 (LOW): 持久化失败前端反馈不一致

**位置**：`stock_workflow.rs:4014-4019`

**问题**：`serenity-screening-completed` 事件在持久化之前发送：

```rust
// 1. 先 emit completed 事件
let _ = app_h.emit("serenity-screening-completed", ...);
// 2. 再持久化到 DB
pick.insert(db).await; // 可能失败
```

如果 DB 持久化失败（约束冲突、连接断开等），前端已经展示 candidates，但数据库中没有记录。下次刷新时候选列表消失。

**影响**：

- 用户体验：看到了候选 → 刷新后消失
- 诊断困难：只有 tracing::warn!() 日志，没有通知前端失败

**修复**：将事件发送移到持久化之后，或者在持久化失败时发送补充的 warning 事件。

---

### D13 (LOW): 7 个基线的布局 x 轴溢出

**位置**：`stock_analysis_setup.rs:5545-5558`

**问题**：基线节点现在从 x=40 延伸到 x=1240。中心节点（trend-scanner 在 x=340，candidate-mapper 在 x=340）与最右侧基线（x=1240）的间距过大。

**影响**：

- 工作流编辑器中模板视觉效果不平衡
- 无功能影响，仅布局美观

**修复**：建议使用多行布局或调整 x 坐标为 2 行排列（每行放 3-4 个基线）。

---

## 整体评估（第二次复查）

| 维度         | 评分 | 说明                                          |
| ------------ | :--: | --------------------------------------------- |
| 修复验证     |  ✅  | 所有 5 个修复正确，代码干净编译，零 warning   |
| 新缺陷严重度 |  ⚠️   | 无 CRITICAL 级新缺陷，D10 为 MEDIUM，其余 LOW |
| 架构健康度   |  📊  | 整体合理，LLM 输出格式零防御是慢性风险        |
| 修改文件质量 |  ✅  | 代码逻辑正确，无回归问题                      |

### 综合状态：6/10 → **8/10**

首次审计修复了结构性缺陷（死代码、缺失边、数据源引用错误、基线覆盖不足、缺少校验），剩余缺陷多为边缘性问题。
