# NPM cache
.npm-cache/
.npm-cache-tmp/
_npx/

# Windows specific
nul
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.next/
next-env.d.ts
*.local
.superpowers
.vscode
.agents
.mcp.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build outputs
dist/
# `target*/` 走 gitignore 语义（无前导斜杠）⇒ 匹配**任意层级**的 `target` 及
# `target-<后缀>` 目录。2026-09-21 半天内两度踩同一个坑：dprint 会读 .gitignore
# 决定扫描面，未被忽略的 target 目录会让它把 cargo 构建产物
# （`.fingerprint/*.json`、`build/<crate>/out/*.md`）当源码检查 ⇒ 门禁**假红**
# （实测 120 个文件不合格，全是构建产物）。
# 先有 `target-v79`（并发会话持锁时的独立 target），后有 `target-pe-fix`，
# 两次都得回头补条目 ⇒ 直接放宽为前缀匹配，一劳永逸。
target*/

# Secrets
*.key

# Planning files
task_plan.md
findings.md
progress.md

# Docs directory
docs/
playwright-report/
.workbuddy/
artifacts/
test-results/

# Local git worktrees
.worktrees/

# Website (VitePress)
website/node_modules/
website/docs/.vitepress/cache/
website/docs/.vitepress/dist/
fix/
autoresearch-*.json
research-*.tsv

debug/*

# AI assistant configs
.arts/
.claude/
.wolf/
.reasonix/
reasonix.toml
.codex/
AGENTS.md

# IDE local settings (use .vscode/ for shared config)
settings.json

# Build / debug output
check_output.txt
replace_logs.py
tmp-*.txt
v15_*.rs
eslint-out.log

# Backup files (临时备份/历史版本)
*.bak
*~
*.orig

# Documentation (local)
Docs/

# Worktrees
.worktrees/test_backup_page*.py

.env
.env.*
*.pem
*.p12
*.pfx
*.jks
credentials.json
service-account*.json
cargo-check-output.txt
.check-i18n-tmp-*

# i18n allowlist backups (temporary files)
scripts/.i18n-allowlist.json.backup*

# Generated scripts
axagent-design-prototype.html
axagent-design-prototype.html.artifact.json
axagent-layout-v2.html
axagent-layout-v3.html
design-prototype.html
design-spec.html
axagent-layout-v4.html
*.output.txt

# Temp debug output (Rust)
/src-tauri/temp/

# Dprint temp cache (should never have been tracked)
/temp/

# Python
__pycache__/
*.pyc

# Generated output files
output/

# AxInvest 专属知识源（lemonhu 数据集体积大，保留在本地/外部存储；sample 目录是测试必需的小样本需提交）
knowledge-sources/lemonhu/
!knowledge-sources/sample/
.codebuddy
# agency-agents 专家源仓库（junction 指向 ../AxOPC/agency-agents-src，本体在 AxOPC 仓库，不入库）
agency-agents-src/

# Local test data
data/

# 微调模型运行时数据（按 UUID 目录生成，不入库）
/src-tauri/crates/agent/fine_tuned_models/

# Rust 临时测试文件
temp_test.exe
temp_test.pdb

# Trae IDE 权限检查临时文件
.trae_permission_check_*

# 临时调试/分析输出（根目录）
/my_errors.txt
/typecheck_out.txt
/typecheck_output.txt
/manual_errors.txt
/errfiles.txt
/dto_sizes.txt
/axagent_dto_fix.ps1
