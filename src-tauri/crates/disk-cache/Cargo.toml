[package]
name = "axagent-disk-cache"
version.workspace = true
edition.workspace = true
publish = false
description = "L2 disk cache backed by SQLite"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { workspace = true, features = ["derive"] }
