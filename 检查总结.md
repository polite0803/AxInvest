# AxInvest 项目检查总结

## 检查完成情况

已全面检查项目三个问题，详细报告已保存至`检查报告.md`。

---

## 一、as-of模式时间泄露检查

**发现严重问题：**

1. **P0: get_quote fall through到live路径** - K线合成失败后返回实时行情数据，存在未来数据泄露
2. **P0: dividend截断缺失** - `get_dividend_records`未调用截断函数

**需修复：**

- 立即修复get_quote的as-of fall through问题
- 补充dividend截断逻辑

---

## 二、股票代码混串检查

**结论：整体设计健壮，未发现严重问题**

**潜在低风险问题：**

1. `get_klines_with_adj`未使用`vendors_for()`路由
2. MCP工具对空stock_code仅用空字符串回退

**设计亮点：**

- 多层隔离：缓存键包含股票代码+as_of后缀
- 自动注入保底机制
- Vendor调用独立，无串扰风险

---

## 三、工作流编辑器布局检查

**结论：未发现容器节点完全不包裹子节点的严重bug**

**中等风险问题：**

1. 容器尺寸常量在两处独立定义（维护隐患）
2. ContainerNode Handle位置使用硬编码回退值
3. GroupFrameNode不具备容器包裹能力

**低风险问题：**

- CSS hover transform可能导致子节点视觉溢出
- 拖拽停止后容器尺寸更新可能滞后

---

## 修复优先级

### 立即修复（P0）

1. as-of模式get_quote fall through问题
2. as-of模式dividend截断缺失

### 高优先级（P1）

3. refresh_correlation/metrics添加AS_OF scope
4. 补充多个方法的as-of降级日志

### 中优先级（P2）

5. 统一容器尺寸常量定义
6. 修正ContainerNode Handle位置回退值

### 低优先级（P3）

7. get_klines_with_adj使用vendors_for路由
8. MCP工具空stock_code检查

---

## 整体评价

项目在三个方面的设计都较为健壮，未发现严重的功能性bug。主要问题集中在：

1. as-of模式的边界情况处理不完善
2. 代码一致性维护（常量定义、路由选择）
3. 一些潜在的竞态条件和视觉细节

建议按照优先级逐步修复，首先解决P0级别的数据泄露问题。
