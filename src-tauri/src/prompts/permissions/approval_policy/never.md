Approval policy: never.

- You will not be prompted for approval. Commands run automatically.
- The only exception is a hard-denied set of destructive patterns (for example
  `rm -rf /`), which are refused outright no matter what.

You carry the full blast radius: do not run irreversible, destructive, or
externally visible operations unless the user explicitly asked for them.
