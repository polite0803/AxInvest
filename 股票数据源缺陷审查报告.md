# 股票数据源缺陷审查与优化分析报告

> **审查日期**: 2026-06-27\
> **审查范围**: `astock-data` crate（核心数据源层）+ `stock-analysis` crate（数据清洗/质量层）\
> **审查方法**: 逐文件代码阅读 + 数据流追踪 + 对比分析

---

## 一、总体结论

AxInvest 的数据源层架构设计优秀——12 个供应商、双层缓存、多级降级链、健康追踪、as-of 时间旅行、浏览器反爬绕过等机制齐全，远超一般个人项目的投入度。**但是**，架构的丰富度也带来了不一致性和技术债：

- **严重缺陷 (Critical)**: 1 个 → 🔄 待大版本重构
- **高优先级 (High)**: 4 个 → H3/H4 ✅ 已修复, H1 🔄 待通用辅助函数, H2 ❌ 复查不成立
- **中优先级 (Medium)**: 4 个 → M1/M2/M3 ✅ 已修复, M4 (功能重复, 低影响)
- **低优先级/优化建议 (Low/Opt)**: 6 个 → 部分在当前修复中已覆盖

---

## 二、缺陷清单

> **修复状态 (2026-06-27 20:38)**: ✅ = 已修复, 🔄 = 待后续大版本重构, ❌ = 经复查不成立

### 🔴 C1 [Critical] — 两套独立 L1 缓存系统并存 🔄

**位置**: `lib.rs:280` + `two_tier_cache.rs`

**描述**: `AStockClient` 内部使用 `cache: RwLock<HashMap<String, (i64, String)>>` 作为 L1 缓存（手工管理过期 + LRU），同时项目中有完整的 `TwoTierCache`（moka L1 + redb L2）实现在 `two_tier_cache.rs` 中。但 **`AStockClient` 并未使用 `TwoTierCache`**。

```rust
// lib.rs:280 — 主数据管道用自己的 HashMap 缓存
cache: RwLock<HashMap<String, (i64, String)>>,

// two_tier_cache.rs — 另一套完整的缓存系统，未被主数据管道使用
pub struct TwoTierCache {
    pub l1: Arc<L1Cache>,   // moka
    pub l2: Option<Arc<L2Cache>>, // redb
}
```

**影响**:

1. 两套缓存分别维护，功能不同步（moka 的 TTL/time-to-idle 等高级功能享受不到）
2. 新人容易引入不一致——到底用哪套？
3. `two_tier_cache.rs` 代码存在但未被引用，是 dead code 风险

**建议**: 将 `AStockClient` 的 HashMap 缓存替换为 `TwoTierCache`，统一缓存栈。

---

### 🟠 H1 [High] — get_news 缺少健康追踪与重试机制 🔄

**位置**: `lib.rs:1354-1413`

**描述**: `get_quote`、`get_klines`、`get_financials` 三个核心方法都使用了 `VendorHealthTracker` 追踪 + 指数退避重试（最多 2 次），但 **`get_news` 完全没有**：

```rust
// get_news — 无 health_tracker，无重试
let mut last_err = None;  // 仅用于记录日志
for name in &news_names {
    // ...一次遍历，失败即跳过
}
// 没有 retry 循环！

// get_quote — 有完整的健康追踪 + 重试
loop {
    let healthy_names = self.health_tracker.try_vendors(&vendor_names).await;
    // ...
    if retry_count < max_retries { /* 指数退避重试 */ }
}
```

**影响**:

- 临时限流场景下 `get_news` 会直接失败，没有机会重试
- 降级 vendor 还会被重复尝试（因为没有 `try_vendors` 过滤）
- 同样的问题存在于：`get_money_flow`、`get_dragon_tiger`、`get_lockup_schedule` 等 20+ 个次要方法

**建议**: 将 VendorHealthTracker + 重试提取为通用 `retry_with_health` 辅助函数，统一应用于所有数据方法。

---

### 🟠 H2 [High] — get_news 存在死变量 ❌

**复查**: `get_news` 中 `last_err` 在 line 1418 用于最终 warn 日志，并非死变量。无 `retry_count`/`max_retries` 声明。此条不成立。

---

### 🟠 H3 [High] — browser_eastmoney 每次请求都做预热 + 1.2s 睡眠 ✅ 已修复

**位置**: `vendors/browser_eastmoney.rs:72-82`

**描述**: 每次调用 `browser_fetch` 都先导航到东方财富首页预热，然后等待 1.2 秒：

```rust
// 每次请求都做：
let warmup_url = "https://www.eastmoney.com/";
f.fetch_text(warmup_url).await;
tokio::time::sleep(std::time::Duration::from_millis(1200)).await;
```

**影响**:

- 每次 API 调用都额外增加 1.2s+ 延迟（预热 + sleep）
- 预热多次无意义——JS 指纹只需设置一次，Cookie 也在浏览器会话中持续有效
- 如果工作流中多次触发 browser_eastmoney（如同时查多个股票的 K 线），总延迟急剧累积

**建议**: 采用惰性预热（Lazy Warmup）——原子标志位检查是否已预热，仅首次执行预热+睡眠。

---

### 🟠 H4 [High] — as-of 模式 quote_from_klines 同步等待 get_financials ✅ 已修复

**位置**: `lib.rs:957-966`

**描述**: as-of 模式合成行情时，为了填充 PE/PB 数据，会**同步阻塞等待** `get_financials` 完成：

```rust
if let Ok(q) = Self::quote_from_klines(stock_code, &ks) {
    // 为填充 PE/PB 等待 get_financials 完成
    if let Ok(fins) = self.get_financials(stock_code).await {  // ← 可能慢！
        if let Some(latest) = fins.into_iter().next() {
            // ...
        }
    }
    return Ok(q);
}
```

`get_financials` 有完整的 2 次重试 + vendor 遍历，在最坏情况下会多等待 30s。而 PE/PB 只是附加信息，财报失败时数据被静默丢弃不影响行情返回。

**影响**:

- as-of 模式下合成行情延迟增加一个完整的 `get_financials` 调用时间
- `get_financials` 失败完全不影响结果，但白白等了

**建议**:

1. 方案 A：移除 PE/PB 填充，行情合成只做行情
2. 方案 B：将 PE/PB 填充改为惰性/可选的独立步骤，不阻塞行情返回

---

### 🔶 M1 [Medium] — 重试 delay 公式不是指数退避 ✅ 已修复

**位置**: `lib.rs:1085`

**描述**: 注释说 "指数退避: 1s, 3s" 但实际公式是线性：

```rust
let delay = retry_count as u64; // retry_count=1 → 1s, retry_count=2 → 2s
```

真正的指数退避应该是 `2^retry_count` 或 `base * multiplier^(retry_count-1)`：

```rust
// 期望: 2s, 4s 或 1s, 3s
let delay = 2u64.pow(retry_count);          // 2s, 4s
// 或
let delay = 1000 + (retry_count - 1) * 2000; // 1s, 3s
```

**影响**: 重试等待时间偏短（2s 影响不大但语义错误），对缓解限流的效果略微降低。

**建议**: 修正为真正的指数退避。

---

### 🔶 M2 [Medium] — FinancialReport::estimated 使用系统时间而非 as-of 时间 ✅ 已修复

**位置**: `types.rs:98`

**描述**: 所有数据源失败时的 fallback 估算报告使用了系统当前时间：

```rust
pub fn estimated(stock_code: &str) -> Self {
    let today = Local::now().format("%Y-%m-%d").to_string();
    // 应使用 crate::as_of::current_as_of() 确定报告日期
```

**影响**:

- as-of 模式下，回放历史日期的分析会得到标注为今天日期的"历史"财务数据
- 时间锚点不准确，可能影响依赖报告日期的下游逻辑

**建议**: 优先使用 `current_as_of()`，fallback 到系统时间。

---

### 🔶 M3 [Medium] — NoHistoricalSemantic 分支是死代码 ✅ 已修复

**位置**: `lib.rs:848-856`

**描述**: `try_vendor_with_asof` 方法中的 `NoHistoricalSemantic` 分支标注为暂不实现：

```rust
AsOfCapability::NoHistoricalSemantic => {
    // Phase 5: 查本地 SQLite 缓存; Phase 1-4 暂不实现
    crate::as_of::record_degradation(
        vendor_name,
        method_name,
        "no historical semantic;本地缓存待 P5 启用",
    );
    None  // 返回 None → 调用方继续遍历下一个 vendor
},
```

而 `daily_snapshot` 系统（`daily_snapshot.rs`）已经实现了全市场数据的每日快照缓存。两者未能对接。

**影响**:

- 所有声明为 `NoHistoricalSemantic` 的方法在 as-of 模式都走 Fallthrough 路径
- daily_snapshot 系统存在但无法被 as-of 路由自动发现
- `try_daily_snapshot` 方法存在于 `AStockClient` 但未被 get_quote/get_klines 等调用

---

### 🔶 M4 [Medium] — `adjustment.rs` 与 `data_clean.rs` 复权功能重复

**位置**: `astock-data/src/adjustment.rs` vs `stock-analysis/src/data_clean.rs`

**描述**: 两个 crate 各自实现了 K 线复权调整逻辑。`data_clean.rs` 的 `adjust_prices` 做前复权，`adjustment.rs` 也做复权调整。

没有实际的代码复用以外的严重后果，但属于功能重复→当复权逻辑需要变更时必须修改两个地方。

---

### 🔹 L1 [Low] — get_quote 重试机制中的变量命名问题

**位置**: `lib.rs:1060-1068`

**描述**: 当 `result.price <= 0.0` 时被记录为失败并 continue，但 `last_err` 虽然被设置了，却和来自 vendor 错误的 `last_err` 混在一起，后面重试时无法区分是"质量不足"还是"vendor 异常"。

---

## 三、优化空间

### 🌟 O1 — 通用 `retry_with_health` 辅助函数

**当前状态**: `get_quote`/`get_klines`/`get_financials` 三处有几乎完全相同的重试+健康追踪逻辑（~50 行各），`get_news` 等 20+ 方法缺失。

**优化方案**: 提取通用闭包辅助函数：

```rust
async fn retry_with_health<T, F>(
    health_tracker: &VendorHealthTracker,
    vendor_names: &[String],
    gate: &DomainGate,
    find_vendor: impl Fn(&str) -> Option<&dyn StockVendor>,
    mut fetcher: F,
) -> Result<T, DataError>
where
    F: FnMut(&dyn StockVendor) -> Result<T, DataError>,
    T: HasDataQuality,  // trait 判断数据质量
```

**收益**: 减少 60% 重复代码，确保所有数据方法行为一致。

---

### 🌟 O2 — 预热缓存化

**当前状态**: `browser_eastmoney` 每次 API 调用都做预热 + 1.2s sleep。

**优化方案**:

```rust
use std::sync::atomic::{AtomicBool, Ordering};

static WARMED_UP: AtomicBool = AtomicBool::new(false);

async fn ensure_warmed_up(fetcher: &dyn BrowserHttpFetch) {
    if WARMED_UP.load(Ordering::Relaxed) {
        return;
    }
    // 执行预热
    let _ = fetcher.fetch_text("https://www.eastmoney.com/").await;
    tokio::time::sleep(Duration::from_millis(1200)).await;
    WARMED_UP.store(true, Ordering::Relaxed);
}
```

**收益**: 预热只在进程生命周期执行一次，后续请求零开销。

---

### 🌟 O3 — 集成 daily_snapshot 到 as-of 路由

**当前状态**: `try_daily_snapshot` 方法存在但未被调用。`NoHistoricalSemantic` 是死代码分支。

**优化方案**: 在 `try_vendor_with_asof` 的 `NoHistoricalSemantic` 分支中查询 `daily_snapshot`：

```rust
AsOfCapability::NoHistoricalSemantic => {
    // 先查每日快照
    if let Some(ctx) = current_as_of() {
        let date = ctx.as_string();
        if let Some(cached) = self.try_daily_snapshot(method_name, &date) {
            return serde_json::from_str(&cached).ok();
        }
    }
    record_degradation(...);
    None
}
```

**收益**: as-of 模式下 NoHistoricalSemantic 方法不再是摆设，可以直接返回历史日期的快照数据。

---

### 🌟 O4 — 统一缓存系统

**当前状态**: HashMap + L1 moka + L2 redb + L2 JSON 四套缓存同时存在。

**优化方案**:

1. 移除 `AStockClient` 的 `cache: RwLock<HashMap<...>>`
2. 用 `TwoTierCache` 替换
3. 将 `DiskCache`（JSON 文件）作为 L2 的可选实现
4. `daily_snapshot` 保留专有 key 空间但复用同一个缓存存储

**收益**: 单套缓存栈，统一 TTL 管理，moka 提供高效的过期/容量控制。

---

### 🌟 O5 — as-of quote 合成的 PE/PB 异步化

**当前状态**: PE/PB 填充阻塞行情返回。

**优化方案 A（移除）**:

```rust
let mut q = Self::quote_from_klines(stock_code, &ks)?;
// PE/PB 延迟到数据聚合层填充，不在数据层处理
return Ok(q);
```

**优化方案 B（并行化）**:

```rust
let mut q = Self::quote_from_klines(stock_code, &ks)?;
// 并行 fetch PE/PB，失败不阻塞
let fins = self.get_financials(stock_code).await.ok();
if let Some(latest) = fins.and_then(|f| f.into_iter().next()) {
    q.pe = latest.eps.filter(|&v| v > 0.0).map(|eps| q.price / eps);
    q.pb = latest.bps.filter(|&v| v > 0.0).map(|bps| q.price / bps);
}
return Ok(q); // 无论 get_financials 是否成功都返回
```

---

### 🌟 O6 — get_news 添加健康追踪 + 重试

**当前状态**: get_news 无健康追踪、无重试。

**优化方案**: 套用 `retry_with_health` 通用辅助函数（见 O1）。

---

## 四、综合优化优先级矩阵

| 编号 | 类型 | 项                          | 影响范围   | 修复难度 | 优先级 |
| ---- | ---- | --------------------------- | ---------- | -------- | ------ |
| C1   | 严重 | 两套缓存系统                | 架构级     | 中       | **P0** |
| H1   | 高   | get_news 缺健康追踪         | 20+ 方法   | 大       | **P0** |
| H3   | 高   | browser_eastmoney 每次预热  | 浏览器路径 | 小       | **P1** |
| H4   | 高   | as-of quote 阻塞等 PE/PB    | as-of 模式 | 小       | **P1** |
| M1   | 中   | 重试 delay 非指数           | 重试逻辑   | 极小     | **P1** |
| M2   | 中   | estimated 用系统时间        | as-of 模式 | 小       | **P2** |
| M3   | 中   | NoHistoricalSemantic 死代码 | as-of 模式 | 中       | **P2** |
| H2   | 高   | get_news 死变量             | 代码质量   | 极小     | **P2** |
| O1   | 优化 | 通用 retry_with_health      | 架构级     | 大       | **P2** |
| O2   | 优化 | 预热缓存化                  | 浏览器路径 | 极小     | **P3** |
| O3   | 优化 | daily_snapshot 集成         | as-of 模式 | 中       | **P3** |
| O4   | 优化 | 统一缓存系统                | 架构级     | 大       | **P3** |

---

## 五、风险监控建议

1. **添加缓存命中率/失败率指标**: 目前没有缓存命中率的监控指标，无法判断缓存策略是否有效
2. **vendor 健康状态的可视化**: `VendorHealthTracker` 的数据已经可以查询（`get_all_health`），但前端没有对应的健康面板
3. **每日快照采集的定期调度**: `DailySnapshotCache` 存在但缺乏自动 sweep 调度器，依赖手动触发
4. **数据源切换的 A/B 测试**: 当引入新 vendor 时，可以保留旧 vendor + 并行为 A/B 测试提供数据
