# AxInvest 项目全面检查报告

## 检查概述

对项目进行了三个方面的全面检查：

1. as-of模式下是否存在时间泄露
2. 股票分析过程中是否存在股票代码遗失、混串
3. 工作流编辑器是否存在布局错误，容器节点不包裹其子节点情况

---

## 一、as-of模式时间泄露检查

### 总体评估

as-of时间旅行系统设计成熟，使用`tokio::task_local!`注入`AsOfContext`，缓存键包含`asof-YYYYMMDD`后缀有效防止缓存污染。但存在若干潜在的时间泄露风险点。

### 发现的问题

#### P0（严重 - 必须修复）

1. **get_quote as-of fall through到live路径**
   - 位置：`lib.rs:758-838`
   - 问题：K线合成失败后fall through到vendor.get_quote()，返回实时行情数据并写入asof缓存
   - 风险：未来数据泄露到as-of模式，无降级日志记录
   - 建议：as-of模式下K线合成失败后应return空结果+record_degradation

2. **truncate_dividend_by_asof是死代码**
   - 位置：`lib.rs:493-504`（死代码）、`lib.rs:1457-1469`（缺失截断）
   - 问题：`get_dividend_records`未调用截断函数，直接返回vendor全量数据
   - 风险：未来日期的股利记录在as-of模式下不会被截断
   - 建议：在`get_dividend_records`中调用`truncate_dividend_by_asof`

#### P1（高 - 建议修复）

3. **refresh_correlation缺少AS_OF scope**
   - 位置：`portfolio_monitor.rs:638` + `stock_analysis.rs:1813-1819`
   - 问题：`get_klines`在无AS_OF scope的上下文中被调用，截断逻辑被跳过
   - 风险：相关性计算使用了未来K线数据
   - 建议：用`with_optional_asof`包裹`refresh_correlation`核心逻辑

4. **refresh_metrics缺少AS_OF scope**
   - 位置：`stock_analysis.rs:1802-1811`
   - 问题：同上，虽然不直接调vendor，但上下文不一致
   - 建议：用`with_optional_asof`包裹

#### P2（中 - 建议改进）

5. **多个Fallthrough方法无降级记录**
   - 位置：`lib.rs`多处
   - 问题：get_lockup/shareholder/research/announcements/block/institutional/north_bound_flow等方法缺少as-of降级日志
   - 建议：补充降级记录

6. **FinancialReport::estimated使用today**
   - 位置：`types.rs:147`
   - 问题：C-fallback估算报告使用`Local::now()`而非as_of_date作为report_date
   - 影响：功能正确（会被截断），但用户体验受影响

### 修复优先级

1. 立即修复：get_quote fall through、dividend截断
2. 高优先级：refresh_correlation/metrics的AS_OF scope
3. 中优先级：补充降级记录

---

## 二、股票代码混串检查

### 总体评估

项目股票代码处理设计健壮，未发现严重的股票代码混淆/串扰问题。

### 检查结果

| 检查项                   | 状态 | 风险等级 |
| ------------------------ | ---- | -------- |
| 股票代码参数传递完整性   | 通过 | 无风险   |
| 多股票并行处理串扰       | 通过 | 无风险   |
| 缓存键包含股票代码       | 通过 | 无风险   |
| Vendor返回数据与请求匹配 | 通过 | 无风险   |
| 工作流内股票代码传递     | 通过 | 无风险   |

### 发现的潜在问题

#### 低风险问题

1. **get_klines_with_adj未使用vendors_for()路由**
   - 位置：`lib.rs:880`
   - 问题：硬编码使用`&self.routing.klines`，replay模式下可能使用非最优vendor顺序
   - 影响：有限，有`truncate_klines_by_asof`兜底
   - 建议：改为`for name in self.routing.vendors_for("klines", &self.routing.klines)`

2. **MCP工具对空stock_code仅用空字符串回退**
   - 位置：`mcp_tools.rs:373, 378`等
   - 问题：当`stock_code`参数缺失或为空时，静默使用空字符串
   - 建议：添加空值检查，返回明确错误

### 设计亮点

- 多层隔离：缓存键 = `method + stock_code + as_of_suffix`
- 自动注入保底：`tool_executor.rs`的`stock_code`自动注入机制
- Vendor独立性：每个vendor调用都是独立的async函数调用
- StockQuote结构体的`code`字段一致性：所有vendor都使用`stock_code.to_string()`赋值

---

## 三、工作流编辑器布局检查

### 总体评估

工作流编辑器的容器节点布局机制设计合理，核心路径（`useFlowNodes.ts`的动态尺寸计算 + ReactFlow的`extent: "parent"`）是正确的。**未发现"容器节点完全不包裹子节点"的严重bug**。

### 发现的问题

#### 中等风险问题

1. **容器尺寸计算与实际渲染存在双重计算路径**
   - 位置：`useFlowNodes.ts:127-171`与`workflowLayout.ts:1100-1103`
   - 问题：容器尺寸常量在两处独立定义
   - 风险：如果任何一处修改而另一处未同步，将导致布局计算不一致
   - 建议：提取共享常量到统一模块

2. **ContainerNode Handle位置使用硬编码回退值**
   - 位置：`ContainerNode.tsx:274-295`
   - 问题：当`data.nodeWidth`/`data.nodeHeight`为undefined时，回退到400x200
   - 风险：Handle连接点与容器视觉边框可能不匹配
   - 建议：确保回退值与`getNodeSize()`一致

3. **GroupFrameNode不具备容器包裹能力**
   - 位置：`GroupFrameNode.tsx`
   - 问题：不被`useFlowNodes.ts`中的`containerStyle`计算逻辑处理
   - 影响：纯视觉分组，不会自动调整尺寸以包含子节点
   - 建议：如需包裹能力，需标记为`isContainer`并增加计算逻辑

#### 低风险问题

4. **CSS hover transform可能导致子节点视觉溢出容器**
   - 位置：`index.css:3923-3926`
   - 问题：容器内子节点hover时放大3%，可能短暂超出容器虚线边框
   - 影响：视觉上的短暂溢出，不影响功能

5. **容器节点最小宽度/高度与默认NODE_SIZE不一致**
   - 位置：`ContainerNode.tsx:87-88`与`workflowLayout.ts:607-610`
   - 问题：CSS最小尺寸（200x80）与布局估算值一致，但与`CONTAINER_MIN_W/H`（400x200）不一致
   - 影响：语义混乱，但实际运行正确

6. **拖拽子节点后容器尺寸更新可能滞后**
   - 位置：`WorkflowEditor.tsx:1334-1517`
   - 问题：快速连续拖拽时可能存在竞态条件
   - 风险：低概率，`suppressRebuildRef`在拖拽结束后50ms内抑制全量重建

### 正确性验证

以下方面经审查是正确的：

1. ReactFlow `parentId` + `extent: "parent"`机制
2. 坐标转换（toRelativePosition/toAbsolutePosition）
3. 容器尺寸动态计算（`useFlowNodes.ts:135-171`）
4. 自动布局中的容器处理（`workflowLayout.ts:1345-1410`）
5. 子节点clamp（`workflowLayout.ts:1165-1203`）
6. 折叠/展开逻辑
7. 拖拽入容器的hit-test

---

## 四、总结与建议

### 优先级排序

#### 立即修复（P0）

1. as-of模式get_quote fall through问题
2. as-of模式dividend截断缺失

#### 高优先级（P1）

3. refresh_correlation/metrics添加AS_OF scope
4. 补充多个方法的as-of降级日志

#### 中优先级（P2）

5. 统一容器尺寸常量定义
6. 修正ContainerNode Handle位置回退值

#### 低优先级（P3）

7. get_klines_with_adj使用vendors_for路由
8. MCP工具空stock_code检查
9. GroupFrameNode包裹能力（功能增强）

### 整体评价

项目在三个方面的设计都较为健壮，未发现严重的功能性bug。主要问题集中在：

1. as-of模式的边界情况处理不完善
2. 代码一致性维护（常量定义、路由选择）
3. 一些潜在的竞态条件和视觉细节

建议按照优先级逐步修复，首先解决P0级别的数据泄露问题，然后逐步完善其他方面。
