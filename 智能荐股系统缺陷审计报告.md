# 智能荐股系统缺陷审计报告

> **审计日期**: 2026-06-28\
> **修复日期**: 2026-06-28 17:40-17:50\
> **审计范围**: `stock-analysis/src/recommender/` 全模块（mod.rs / types.rs / scoring.rs / strategy.rs / pool.rs / indicators.rs + 6 子策略）\
> **代码行数**: 约 2,100 行 Rust\
> **现有覆盖**: 此子系统**未被**之前的任何审计报告覆盖（股票分析工作流审查报告、数据源审查报告均未涉及 recommender）

---

## 审计结论

| 级别         | 数量 | 核心问题域                                          |
| ------------ | ---- | --------------------------------------------------- |
| **CRITICAL** | 4    | 数据持久化缺失 / 指标计算错误 / 缓存体系漏洞        |
| **HIGH**     | 4    | 资源浪费 / 重复代码 / 误导性信息 / 格式脆弱         |
| **MEDIUM**   | 5    | 排序确定性 / 空结果缓存 / 参数语义混淆 / 未使用变量 |
| **LOW**      | 2    | 配置过滤失效 / 缓存同步遗漏                         |

---

## CRITICAL 缺陷

### C1 — recommend_stocks 输出未持久化到 reco_picks 表

**文件**: `src-tauri/crates/stock-analysis/src/recommender/mod.rs:268-626`

**发现**: `recommend_stocks()` 函数完成扫描→去重→分组的全流程后，仅将结果写入内存缓存 `RESULT_CACHE` 并返回前端，**从不写入 `reco_picks` 数据库表**。

**证据**:

- reco_picks 表中现有的 INSERT 路径仅在 `stock_workflow.rs` 的 `run_serenity_screening()` 中（style="serenity"）
- 其他 5 个风格（trend / value / capital / reversion / watchlist）在表结构中有列定义，但**没有任何代码写入**它们
- `recommend_stocks` 函数中没有任何 DB 写入逻辑

**影响**:

1. **重启丢失**: 应用重启后所有荐股结果丢失（仅靠 60s TTL 内存缓存）
2. **前端历史面板空**: 用户打开荐股历史面板（`list_reco_history`）只能看到 Serenity 候选
3. **回测不可用**: `backtest_reco_strategies` 读取最新一次 `reco_picks` 记录做正向/负向样本分离时，只能拿到 Serenity 候选，trend/value/capital/reversion/watchlist 的回测数据为零
4. **定期荐股（Cron）无历史追踪**: cron 调度执行的推荐不会留下可追溯的记录

**修复建议**:

- 在 `recommend_stocks()` 返回结果前，将各风格 picks 持久化到 `reco_picks` 表
- 保留 `generated_at` 用于分组，`seed_pool_json` 用于快照
- 注意已写入的 serenity 记录需要与正常推荐做 generated_at 区分

---

### C2 — capital.rs scan_from_klines 量比计算取最早 K 线而非最新

**文件**: `src-tauri/crates/stock-analysis/src/recommender/strategies/capital.rs:52-56`

```rust
// 问题代码:
let avg_vol_5 = volumes_5.iter().sum::<f64>() / volumes_5.len() as f64;
let avg_vol_20: f64 = klines.iter().take(20).map(|k| k.amount).sum::<f64>() / 20.0;  // ← 取最旧20根
let vol_ratio = if avg_vol_20 > 0.0 { avg_vol_5 / avg_vol_20 } else { 1.0 };
```

**发现**: `avg_vol_5` 通过 `klines.iter().rev().take(5)` 取**最新的** 5 根 K 线计算，但 `avg_vol_20` 通过 `klines.iter().take(20)` 取**最旧的** 20 根 K 线（因 `get_klines` 按日期升序返回）。量比 = 最新 5 日均量 / 最旧 20 日均量，**不是标准意义的量比**。

**对比 TrendStrategy**（第 68-75 行，实现正确）:

```rust
let avg_20 = indicators::avg_amount_20d(&klines).unwrap_or(0.0);  // 内部用 rev().take(20)
let today_amount = klines.last().map(|k| k.amount).unwrap_or(0.0);
let turnover_anomaly = if avg_20 > 0.0 { today_amount / avg_20 } else { 1.0 };
```

**影响**: `vol_ratio` 计算错误，导致：

- 真实放量的股票可能被错误地判为量比不足而跳过
- 不放量的股票可能因与"最旧 20 天"比较而恰好通过

**修复建议**: 将 `take(20)` 改为 `rev().take(20)`，或复用 `indicators::avg_amount_20d()`。

---

### C3 — Serenity 缓存（SERENITY_SEED / SERENITY_CANDIDATE_CACHE）无 TTL 和 as-of 隔离

**文件**: `src-tauri/crates/stock-analysis/src/recommender/mod.rs:41-83`

```rust
static SERENITY_SEED: LazyLock<RwLock<Vec<SeedItem>>> = LazyLock::new(|| RwLock::new(Vec::new()));
static SERENITY_CANDIDATE_CACHE: LazyLock<RwLock<HashMap<String, serde_json::Value>>> =
    LazyLock::new(|| RwLock::new(HashMap::new()));
```

**发现**:

1. 两个静态变量**没有写入时间戳**和 TTL 检查
2. **没有 as-of 上下文 key 隔离**：live 模式和 replay 模式共用同一份缓存
3. 一旦写入，永久有效直到被下一次写入覆盖

**影响**:

- Serenity workflow 几周没运行 → 推荐结果仍包含过期的瓶颈分析候选
- as-of 模式下可能返回与当前 as_of_date 不匹配的候选数据
- 用户删除了 DB 中的 serenity pick 记录但全局缓存仍在 → 下次推荐仍旧出现

**修复建议**:

- 给 Serenity 缓存增加 `Instant` 时间戳和 TTL（建议 1h）
- 增加 as-of key: `HashMap<(Period, Option<String>), ...>` 区分 live/replay
- `clear_serenity_candidate_cache()` 应在删除操作后自动调用

---

### C4 — invalidate_cache() 不清空 Serenity 缓存

**文件**: `src-tauri/crates/stock-analysis/src/recommender/mod.rs:255-259`

```rust
pub fn invalidate_cache() {
    let mut g = RESULT_CACHE.write().unwrap_or_else(|e| e.into_inner());
    g.clear();
    clear_cached_vendors();  // 只清了 vendor 和结果缓存
    // 没有调用 clear_serenity_candidate_cache() 或清空 SERENITY_SEED
}
```

**发现**: `invalidate_cache()` 被设计为"设置页保存 vendor 后清缓存"。但它只清 `RESULT_CACHE` 和 `VENDOR_CACHE`，**不清** `SERENITY_SEED` 和 `SERENITY_CANDIDATE_CACHE`。

**影响**:

- 用户在设置页切换 vendor 配置 → 结果缓存刷新了 → 但 Serenity 候选股仍混在新的推荐结果中
- 用户可能会看到"之前已排除的"Serenity 候选股

**修复建议**: 在 `invalidate_cache` 末尾追加 `clear_serenity_candidate_cache()`。

---

## HIGH 缺陷

### H1 — SerenityStrategy 对全 seed pool 发起 financials/quote/klines API 请求

**文件**: `src-tauri/crates/stock-analysis/src/recommender/strategies/serenity.rs:345-357`

```rust
async fn scan(&self, ctx: &RecoContext<'_>) -> Result<Vec<RecoPick>, String> {
    let mut picks = Vec::new();
    for (code, name, sector) in ctx.seed {  // ← 遍历整个 seed pool (~200 只)
        // 每只都调:
        // - get_serenity_candidate_detail(code)  // 194/200 返回 None
        // - client.get_financials(code)           // 194 次浪费
        // - client.get_quote(code)                // 194 次浪费
        // - client.get_klines_with_adj(code)      // 194 次浪费
    }
}
```

**发现**: Serenity 策略遍历整个 seed pool（约 200 只股票），但实际有 serenity 缓存数据的通常只有 6-10 只（serenity-screening workflow 的输出）。其余 190 只的 `serenity_score = 0`，大概率在 3 路校验（毛利率/负债率/营收增速）中被提前返回 None——**但仍然发送了 190 次 financials + 190 次 quote + 190 次 klines API 请求**。

**影响**:

- 每次荐股额外增加 **~600 次 vendor API 调用**
- 增加推荐延迟（串行锁下每只~200ms→总延迟约 40s）
- 增加 vendor 被限流的概率

**修复建议**: 提前检查 `get_serenity_candidate_detail(code)` 是否返回 Some，为 None 时直接 `continue`，跳过 financials/quote/klines 请求。

---

### H2 — WatchlistStrategy 与 emit_synthetic_picks 大量重复代码

**文件**:

- `src-tauri/crates/stock-analysis/src/recommender/strategies/watchlist.rs:46-168`（WatchlistStrategy::scan_one）
- `src-tauri/crates/stock-analysis/src/recommender/strategies/watchlist.rs:251-385`（scan_synthetic_one）

**发现**: Watchlist 策略与兜底合成 picks 的代码几乎完全一致——相同的 price 乘数计算方式、相同的周期参数结构、相同的 `calc_confidence` / `calc_position` 调用模式。唯一的区别是：

- `base_position`: Watchlist(2/4/6/8) vs Synthetic(2/4/6/8) → **完全一样**
- `calc_confidence` 硬编码参数: Watchlist(0.55/0.5/0.5/0.0/1.0) vs Synthetic(0.45/0.4/0.4/0.0/1.0)

**影响**:

- 修改入场/止损/目标参数需同时改两处，极易漏改
- 代码行数膨胀（Watchlist: ~230 行 + Synthetic: ~135 行）

**修复建议**: 将公共的价格乘数计算逻辑抽取为辅助函数，两个策略调用同一函数并传入差异化参数。

---

### H3 — TrendStrategy risk_notes 硬编码误导性风险提示

**文件**: `src-tauri/crates/stock-analysis/src/recommender/strategies/trend.rs:246`

```rust
risk_notes: vec!["大盘破 20 日均线 -10%".to_string()],
```

**发现**: 所有趋势策略的 RecoPick 都带 "大盘破 20 日均线 -10%" 的风险提示，但 **没有任何大盘状态检查逻辑**。无论大盘处于什么状态（牛市/震荡/下跌），提示始终存在。

**影响**:

- 用户在看好个股的同时看到"大盘破均线"的提示，产生认知矛盾
- 降低风险提示的可信度：当所有推荐都带同一句风险提示时，提示失去了区分度

**修复建议**: 在 `scan_one` 中拉取大盘指数（如沪深 300 的 K 线），检查当前指数是否在 20 日均线附近。仅在确实破位时才输出此提示。或改为泛化的 "个股回调/跌破均线风险"。

---

### H4 — RecoCron 的 last_picks_count 解析格式脆弱

**文件**: `src-tauri/src/commands/recommendation_cron.rs:149-154`

```rust
let last_picks_count = j.last_result.as_ref().and_then(|r| {
    r.output
        .as_deref()
        .and_then(|s| s.strip_prefix("picks="))
        .and_then(|s| s.parse::<usize>().ok())
});
```

**发现**: `picks_to_run_output` 返回 `format!("picks={}", picks.len())`，`row_from_job` 用 `strip_prefix("picks=")` 反向解析。如果 future 版本添加了额外输出（如 `picks=15, duration=2.3s`），解析器会静默返回 None。

**影响**: 前端 cron 行 `lastPicksCount` 字段静默变成 null → 用户看不到该 cron 的推送数量。

**修复建议**: 使用 JSON 输出格式（如 `{"picks": 15}`）并在解析时容忍额外字段，或使用结构化字段存储。

---

## MEDIUM 缺陷

### M1 — RecoResponse.picks HashMap 迭代顺序不确定

**文件**: `src-tauri/crates/stock-analysis/src/recommender/types.rs:130`

```rust
pub picks: std::collections::HashMap<Style, Vec<RecoPick>>,
```

**发现**: `RecoResponse.picks` 使用 `HashMap<Style, Vec<RecoPick>>`，HashMap 的 `IntoIter` 迭代顺序不确定。前端渲染 6 个风格区块时，每次刷新风格顺序可能不同。

**影响**: 用户感知不一致——"上次 trend 在第一行，这次怎么跑到第三行了"

**修复建议**: 改为 `IndexMap<Style, Vec<RecoPick>>`，或在前端按 `Style.as_str()` 排序后渲染。

---

### M2 — 空结果不缓存导致高频重复扫描

**文件**: `src-tauri/crates/stock-analysis/src/recommender/mod.rs:244-249`

```rust
let all_empty = resp.picks.values().all(|v| v.is_empty());
if all_empty {
    return;  // 空结果不缓存
}
```

**发现**: 当所有 vendor 同时不可用时，推荐结果为全空，`cache_put` 跳过。下一次请求缓存 miss → 重新扫描 → 又全空 → 又不缓存，形成**每次请求都扫描 vendor 但每次都不缓存**的死循环。

**影响**: vendor 故障期间每次荐股请求都引发完整的扫描流程（~2000 次 vendor API 调用），加速 vendor 过载。

**修复建议**: 空结果也缓存（设置更短的 TTL，如 30s），避免 vendor 故障期间重复打 vendor。

---

### M3 — calc_confidence turnover_anomaly 参数语义不一致

**文件**:

- `trend.rs:222-228` → `turnover_anomaly` = today_amount / avg_20 ✅
- `value.rs:205-211` → 第 5 个参数是 `val_conf_base` 但被赋值到 `turnover_anomaly` ❌
- `capital.rs:287-293` → 第 5 个参数是 `cap_conf_base` 但被赋值到 `turnover_anomaly` ❌
- `serenity.rs:225` → 硬编码 1.0（无量比信息）
- `watchlist.rs:138-144` → 硬编码 1.0
- `emit_synthetic_picks` → 硬编码 1.0

**发现**: `calc_confidence` 函数的第 5 个参数是 `turnover_anomaly`（量比异常度），但：

- ValueStrategy 传入 `val_conf_base`（名称暗示是基础置信度而非量比）
- CapitalStrategy 传入 `cap_conf_base`（同上）

这些策略根本没有计算真实的量比数据，而是传了一个无意义的参数名。

**影响**: 所有这些策略的 `turnover_anomaly` 惩罚逻辑无法生效（恒为 1.0 或固定值），量比异常惩罚仅 TrendStrategy 有效。

**修复建议**: 将参数重命名为更明确的名称（如 `turnover_factor`），或在参数文档中标注默认值语义。在 value/capital/serenity/watchlist 中真正计算量比数据。

---

### M4 — 推荐历史删除后 Serenity 缓存不同步

**文件**: `src-tauri/src/commands/stock_workflow.rs:4680-4717`

**发现**: `batch_delete_reco_history` 和 `delete_serenity_pick` 只从 DB 删除记录，不调用 `clear_serenity_candidate_cache()` 或更新 `SERENITY_SEED`。

**影响**: 用户在 Serenity 面板删除了一条候选股 → 下次荐股时仍然出现在推荐结果中（因为缓存未刷新）。

---

### M5 — `empty_styles` 变量被计算但未使用

**文件**: `src-tauri/crates/stock-analysis/src/recommender/mod.rs:544-558`

```rust
let empty_styles: Vec<_> = [Style::Trend, ...].iter()
    .filter(|s| by_style.get(s).is_none_or(|v| v.is_empty()))
    .copied()
    .collect();
if !empty_styles.is_empty() {
    tracing::info!("[recommender] empty buckets before synthetic fill: {:?}", empty_styles);
}
```

**发现**: `empty_styles` 被计算后仅用于日志。后续的合成兜底逻辑并不使用这个变量，而是遍历 `[Style::Trend, ..., Style::Serenity]` 独立判断。

**影响**: 不必要的计算开销 + 日志中记录了一个不会被逻辑使用的变量。

---

## LOW 缺陷

### L1 — RecoCronConfig 的 min_confidence / top_n 在 cron 执行中无效

**文件**: `src-tauri/src/commands/recommendation_cron.rs:33-37`

**发现**: `RecoCronConfig` 包含 `min_confidence` / `top_n` 字段，但在 cron executor 调用 `recommend_stocks` 后，输出的是整个 `Vec<RecoPick>` 然后 `picks_to_run_output(picks.len())`。`min_confidence` 和 `top_n` 过滤没有在 cron 执行链路中被使用。

**影响**: 用户配置"最低置信度 70，推送 Top 5"但实际上可能收到 30 条低置信度推荐。

---

### L2 — FALLBACK_STOCKS 硬编码 63 只股票可能过时

**文件**: `src-tauri/crates/stock-analysis/src/recommender/pool.rs:14-63`

**发现**: `FALLBACK_STOCKS` 包含 63 只硬编码股票代码/名称。这些股票的代码可能因重组、退市、变更而失效（如盐湖股份曾暂停上市），但代码中没有任何"检查代码是否仍上市"的逻辑。

**影响**: 种子池包含退市/ST 风险股票（虽然流动性过滤会筛除 ST），但流动性过滤失败时（如少量 ST 被保留），兜底列表会推荐无效标的。

---

## 综合评价

| 维度               | 评分   | 说明                                                  |
| ------------------ | ------ | ----------------------------------------------------- |
| **数据完整性**     | ⚠️ 3/10 | 核心荐股结果不落库，重启即丢                          |
| **指标计算正确性** | ⚠️ 5/10 | capital 量比计算错误，多策略 turnover 参数语义混淆    |
| **缓存体系**       | ⚠️ 4/10 | Serenity 缓存无 TTL、无 as-of 隔离、删除不同步        |
| **资源效率**       | ⚠️ 6/10 | SerenityStrategy 全 pool 扫描浪费 95% API 调用        |
| **代码质量**       | ⚠️ 6/10 | Watchlist/Synthetic 大量重复，trend risk_notes 硬编码 |
| **定时任务**       | ⚠️ 5/10 | min_confidence/top_n 配置不生效，输出格式脆弱         |
| **前端一致性**     | ⚠️ 7/10 | HashMap 顺序不确定(前端可兜底排序)                    |

**总体评分: 5.1/10** — 核心机制可用但存在多个数据完整性、计算正确性、资源效率方面的中高风险缺陷。

---

## 修复状态（2026-06-28 17:40-17:50）

所有修复已在 `cargo check --package axagent` 零错误零警告下验证。

| 缺陷                                | 严重度   |   状态    | 修改内容                                                                 |
| ----------------------------------- | -------- | :-------: | ------------------------------------------------------------------------ |
| C1 荐股不落库                       | CRITICAL | ❌ 假阳性 | 实际 `stock_analysis.rs:2850-2906` 已有持久化                            |
| C2 量比取最早 K 线                  | CRITICAL |    ✅     | `capital.rs:55` `take(20)` → `rev().take(20)`                            |
| C3 Serenity 缓存无 TTL              | CRITICAL |    ✅     | `mod.rs` 新增 `SERENITY_CACHE_TIME` + `is_serenity_cache_fresh()` 1h TTL |
| C4 `invalidate_cache` 不清 Serenity | CRITICAL |    ✅     | 末尾追加 `clear_serenity_candidate_cache()`                              |
| H1 Serenity 全 pool 发 API          | HIGH     |    ✅     | 无候选缓存时直接 `return None`，跳过 financials/quote/klines             |
| H2 Watchlist/Synthetic 重复         | HIGH     |    ⏳     | 重构量较大，后续抽公共函数                                               |
| H3 trend risk_notes 硬编码          | HIGH     |    ✅     | 改为 "个股回调/跌破短期均线风险"                                         |
| H4 cron output 解析脆弱             | HIGH     |    ✅     | 改为 JSON 格式 `{"picks": N}` + 兼容旧格式                               |
| M1 HashMap 顺序不确定               | MEDIUM   |    ✅     | `picks` 改为 `BTreeMap`；`Style` 派生 `PartialOrd, Ord`                  |
| M2 空结果不缓存                     | MEDIUM   |    ✅     | 移除空结果跳过逻辑，空结果也缓存（同 60s TTL）                           |
| M3 turnover 参数语义混淆            | MEDIUM   |    ✅     | `value.rs`/`capital.rs` 显式传 `1.0` 替代模板变量误用                    |
| M4 删除历史不清缓存                 | MEDIUM   |    ✅     | `delete_serenity_pick` 追加 `clear_serenity_candidate_cache()`           |
| M5 `empty_styles` 未使用            | MEDIUM   |    ✅     | 移除未使用的变量块                                                       |
| L1 cron 配置不生效                  | LOW      |    ✅     | `services.rs` 新增 `stock-recommendation` cron handler                   |
| L2 兜底列表可能过时                 | LOW      |    ⏳     | 需定期维护，实际有流动性过滤兜底                                         |

### 升级修复（cron 执行器缺失 `stock-recommendation` 分支）

`services.rs` 的 cron executor handler 原本只处理了 `watchlist-scan`/`portfolio-scan`/`validate-decisions`/`batch-reflection` 四个分支，**`stock-recommendation` 分支完全缺失**。这意味着通过 `create_recommendation_cron` 创建的荐股定时任务**从未被实际调度执行**。已新增完整 handler（~90 行），包含：

1. 从 `prompt` 解析 `RecoCronConfig`（periods / min_confidence / top_n）
2. 读取 `workflow_template` 的 `variables`
3. 遍历各 period 调用 `recommender::recommend_stocks()`
4. 应用 `min_confidence` 过滤和 `top_n` 截断
5. 以 JSON 格式记录输出（`{"picks":N, "success":bool, "errors":[...], "periods":[...]}`）

### 补充修复

- `commands/mod.rs` + `lib.rs` 注册 `recommendation_cron` 模块和 Tauri 命令（此前不存在，导致编译失败）
- `delete_serenity_pick` 同步清空全局缓存（避免 DB 已删但荐股仍出现）
