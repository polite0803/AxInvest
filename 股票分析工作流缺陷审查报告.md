# 股票分析工作流缺陷审查报告

> 审查范围：stock-analysis 工作流全链路（数据采集 → 多空辩论 → 风险评估 → 最终决策）
> 审查日期：2026-06-26
> 审查人：助理

## 执行摘要

本次审查覆盖工作流定义（`stock_analysis_setup.rs`）、运行时执行（`stock_workflow.rs`）、决策公式（`portfolio-mgr.rhai`）、变量路径解析（`var_filter.rs`）、节点执行器（`agent_executor.rs` / `aggregator_executor.rs`）以及前端风险雷达图（`RiskMatrix.tsx`）。

**发现 4 个 CRITICAL 级缺陷、3 个 HIGH 级缺陷、2 个 MEDIUM 级缺陷。**

其中 4 个 CRITICAL 缺陷**全部属于同一类系统性问题**：`input_mapping` 的变量路径与节点实际输出结构不匹配，导致结构化参数注入静默失效，LLM/公式只能依赖非结构化文本或默认值做决策。这正是项目记忆中 V29 修复过的问题，但在 `build_analyst_input_mapping`、`data-quality`、`portfolio-mgr` 三处遗漏未修。

---

## 缺陷总览

| 编号 | 级别     | 位置                                | 一句话描述                                                                     |
| ---- | -------- | ----------------------------------- | ------------------------------------------------------------------------------ |
| C1   | CRITICAL | `stock_analysis_setup.rs:4404-4407` | `build_analyst_input_mapping` 用 `.params.` 路径，AgentNode 无 params 字段     |
| C2   | CRITICAL | `stock_analysis_setup.rs:2184-2204` | `data-quality` 用 `.confidence` 顶层路径，实际在 `.content.` 内                |
| C3   | CRITICAL | `stock_analysis_setup.rs:2366`      | `portfolio-mgr` 用 `data-quality.result.score`，AgentNode 无 result 字段       |
| C4   | CRITICAL | `stock_analysis_setup.rs:2252-2254` | `research-mgr` 用 `risk-aggregated.result.aggressive_pct`，result 是数组非对象 |
| H1   | HIGH     | `stock_analysis_setup.rs:2256,2324` | `research-mgr`/`trader` 引用 `debate-convergence` 但无 context_sources/显式边  |
| H2   | HIGH     | `portfolio-mgr.rhai:47`             | 共识因子 f2_weight 复用 "trend" 键，与 f1_weight 相同                          |
| H3   | HIGH     | `portfolio-mgr.rhai:212`            | `evidence_pct` 分母硬编码 1.0，理论最大权重约 1.20                             |
| M1   | MEDIUM   | `RiskMatrix.tsx:282-321`            | 风险雷达图 fallback 仍可能因关键词累加逼近 100                                 |
| M2   | MEDIUM   | `portfolio-mgr.rhai` 全文           | LLM 失败级联：上游 LLM 失败时 posterior 退化为先验                             |

---

## CRITICAL 级缺陷（决策级致命问题）

### C1: `build_analyst_input_mapping` 路径错误，30+ 个结构化分数注入失效

**位置**：`src-tauri/src/commands/stock_analysis_setup.rs:4398-4412`

**问题**：

```rust
fn build_analyst_input_mapping(a_ids: &[&str]) -> std::collections::HashMap<String, String> {
    let mut map = HashMap::new();
    for aid in a_ids {
        let prefix = aid.strip_prefix("a-").unwrap_or(aid);
        map.insert(format!("{prefix}_bull_score"), format!("{aid}.params.bull_score"));
        map.insert(format!("{prefix}_bear_score"), format!("{aid}.params.bear_score"));
        map.insert(format!("{prefix}_consensus"), format!("{aid}.params.consensus_score"));
    }
    map.insert("stock_lessons".into(), "stock_lessons".into());
    map
}
```

路径用的是 `{aid}.params.bull_score`，但 AgentNode 实际输出结构是（见 `agent_executor.rs:984-990`）：

```json
{
  "role": "...", "model": "...",
  "content": "<json_string>",  // LLM 输出的 JSON 被序列化为字符串
  "thinking": null, "usage": {...}, "tool_calls_made": [...], "node_id": "..."
}
```

AgentNode **没有 `params` 字段**。`resolve_var_path` 解析 `a-market-analyst.params.bull_score` 时：

1. root = `a-market-analyst` → 取出 AgentNode 输出对象
2. part = `params` → 对象无此字段 → 返回 `None`
3. fallback flat key 查找 → 不存在
4. 最终返回 `None`

**影响范围**（所有调用 `build_analyst_input_mapping` 的节点）：

- `bull-r1` / `bear-r1`（首轮辩手）
- `bull-r2` / `bear-r2` / `bull-r3` / `bear-r3`（后续轮辩手）
- `debate-convergence`（辩论收敛）
- `risk-agg` / `risk-con` / `risk-neu`（三档风险评估）

共 **10 个分析师 × 3 个字段 × 9 个节点 = 270 个 input_mapping 条目全部失效**。

**后果**：辩手和风险评估节点拿不到结构化的多空分数，LLM 只能从 `context_sources` 注入的非结构化文本中自行提取，分析质量和一致性下降。

**修复**：

```rust
map.insert(format!("{prefix}_bull_score"), format!("{aid}.content.bull_score"));
map.insert(format!("{prefix}_bear_score"), format!("{aid}.content.bear_score"));
map.insert(format!("{prefix}_consensus"), format!("{aid}.content.consensus_score"));
```

> 注：`resolve_var_path` 遇到 `Value::String` 会自动 `from_str` 解析（V29 修复），所以 `.content.bull_score` 能正确下钻到 JSON 字符串内部字段。

---

### C2: `data-quality` input_mapping 路径错误，DQI 信心值注入失效

**位置**：`src-tauri/src/commands/stock_analysis_setup.rs:2182-2208`

**问题**：

```rust
a.config.input_mapping = [
    // 注释错误：声称"Json 模式直接是解析后的 JSON 对象，无 .params 包装"
    ("mk_confidence", "a-market-analyst.confidence"),
    ("sent_confidence", "a-sentiment.confidence"),
    // ... 10 个 confidence + 10 个 if_data_gaps，全部用顶层路径
    ("mk_data_gaps", "a-market-analyst.if_data_gaps"),
    // ...
]
```

注释错误地认为 AgentNode 的 Json 模式输出是"直接解析后的 JSON 对象"。实际上 `agent_executor.rs` 把 LLM 输出原样放在 `content` 字段（JSON 字符串），confidence/if_data_gaps 等业务字段嵌套在 content 内部。

`resolve_var_path("a-market-analyst.confidence", variables)` → 对象无顶层 `confidence` 字段 → 返回 `None`。

**影响**：`data-quality` 节点的 20 个 input_mapping 条目（10 confidence + 10 if_data_gaps）全部失效。DQI 只能从 context_sources 注入的全文中用关键词匹配判断"信心低迷"等条件。

**修复**：

```rust
("mk_confidence", "a-market-analyst.content.confidence"),
("sent_confidence", "a-sentiment.content.confidence"),
// ... 所有 confidence 和 if_data_gaps 路径加 .content.
("mk_data_gaps", "a-market-analyst.content.if_data_gaps"),
// ...
```

---

### C3: `portfolio-mgr` 的 dqi_score 路径错误，数据质量因子不参与决策

**位置**：`src-tauri/src/commands/stock_analysis_setup.rs:2366`

**问题**：

```rust
input_mapping: [
    ("totalScore", "t-scoring.result.totalScore"),          // ✓ ToolNode 有 result 字段
    ("dqi_score", "data-quality.result.score"),             // ✗ AgentNode 无 result 字段
    ("factor_weights", "t-scoring.result.factor_backtest.factors"),
    // ...
]
```

`data-quality` 是 AgentNode（通过 `agent()` 辅助函数创建），输出结构是 `{role, model, content, ...}`，**没有 `result` 字段**。`data-quality.result.score` 解析失败 → `dqi_score` 永远为 `None`。

**对 Rhai 决策公式的影响**（`portfolio-mgr.rhai:71-73`）：

```rhai
let f5_weight = if present(dqi_score) { get_weight(f_weights, "bias", f5_default) } else { 0.0 };
let f5_signal = if f5_weight > 0.0 { (dqi_score - 50.0) / 50.0 } else { 0.0 };
```

`dqi_score` 不存在 → `f5_weight = 0.0` → **"因子5: 数据质量"完全不参与贝叶斯融合**。

**后果**：即使数据质量极差（DQI 评级 F、覆盖度不足），portfolio-mgr 也不会降低置信度或仓位。决策系统对数据质量"失明"。

**修复**：

```rust
("dqi_score", "data-quality.content.score"),
```

---

### C4: `research-mgr` 的 risk-aggregated 路径错误，三档风险仓位注入失效

**位置**：`src-tauri/src/commands/stock_analysis_setup.rs:2252-2254`

**问题**：

```rust
a.config.input_mapping = [
    ("overall_risk", "risk-level.category"),                          // ✓ LlmClassifier 有 category
    ("agg_risk_pos", "risk-aggregated.result.aggressive_pct"),        // ✗ result 是数组
    ("cons_risk_pos", "risk-aggregated.result.conservative_pct"),     // ✗ result 是数组
    ("neut_risk_pos", "risk-aggregated.result.neutral_pct"),          // ✗ result 是数组
    // ...
]
```

`agg-risk` 是 AggregatorNode，`strategy: "all"`。根据 `aggregator_executor.rs:114-116`，"all" 策略的 result 是 **JSON 数组**：

```json
{
  "strategy": "all",
  "source_count": 3,
  "result": [
    { "role": "risk-agg", "content": "<json_string>", ... },
    { "role": "risk-con", "content": "<json_string>", ... },
    { "role": "risk-neu", "content": "<json_string>", ... }
  ],
  "node_id": "agg-risk"
}
```

`resolve_var_path` 用 `Value::get(part)` 导航，`Value::Array` 的 `.get("aggressive_pct")` 返回 `None`（数组下标需要 usize，字符串 key 无法匹配）。

**影响**：research-mgr 拿不到三档风险偏好的仓位建议（aggressive_pct/conservative_pct/neutral_pct），LLM 只能从 context_sources 注入的聚合数组文本中自行提取。

**修复方案**（二选一）：

方案 A（推荐）：改 AggregatorNode strategy 为 `"merge"`，让 result 成为合并后的对象。但需要 risk-agg/risk-con/risk-neu 三个 AgentNode 的 content JSON 中包含 `aggressive_pct`/`conservative_pct`/`neutral_pct` 字段，且 merge 会浅合并。

方案 B：在 research-mgr 的 input_mapping 中直接引用原始 AgentNode：

```rust
("agg_risk_pos", "risk-agg.content.aggressive_pct"),
("cons_risk_pos", "risk-con.content.conservative_pct"),
("neut_risk_pos", "risk-neu.content.neutral_pct"),
```

但需要确认 risk-agg 等 AgentNode 的 context_sources/显式边是否覆盖到 research-mgr。

> 注：`resolve_var_path` 不支持数组下标导航（如 `result.0.content`），因为 `Value::Array::get(&str)` 无法按数字字符串取索引。

---

## HIGH 级缺陷（决策质量退化）

### H1: `research-mgr`/`trader` 引用 `debate-convergence` 但无 context_sources/显式边

**位置**：`stock_analysis_setup.rs:2248-2261`（research-mgr）、`2323-2329`（trader）

**问题**：

```rust
// research-mgr
a.config.context_sources = vec![
    "value-investor", "t-scoring", "t-valuation", "t-risk",
    "risk-aggregated", "risk-level",
    // 缺少 "debate-convergence"
];
a.config.input_mapping = [
    // ... 但这里引用了 debate-convergence
    ("consensus_score", "debate-convergence.content.consensus_score"),
];

// trader
a.config.context_sources = vec!["research-mgr"];  // 只有 research-mgr
a.config.input_mapping = [
    ("consensus_score", "debate-convergence.content.consensus_score"),  // 无 context_sources
    ("stock_lessons", "stock_lessons"),
];
```

违反项目记忆中的规则："context_sources 引用的上游节点必须有直接边到当前节点/容器"。

**当前为何能工作**：`engine.rs:1996-2008` 的 `state.variables` 全局合并机制使所有已执行节点输出对下游可见。`debate-convergence` 通过 `debate-convergence → value-investor → research-mgr → trader` 的间接时序链保证在 trader 执行前完成，`resolve_var_path` 能从 `state.variables` 找到。

**风险**：

- 若未来调度策略变更（如并行化优化），`debate-convergence` 可能未在 trader 执行前完成
- `agent_executor.rs:416-439` 的 context_sources 注入逻辑会因找不到 source 而记录 `missing_sources` 警告（虽然 input_mapping 走的是另一条路径）
- 违反显式依赖原则，可维护性差

**修复**：

```rust
// research-mgr
a.config.context_sources = vec![
    "value-investor", "t-scoring", "t-valuation", "t-risk",
    "risk-aggregated", "risk-level",
    "debate-convergence",  // 新增
];

// trader
a.config.context_sources = vec!["research-mgr", "debate-convergence"];  // 新增

// 或加显式边
edges.push(edge("e-debate-convergence-research-mgr", "debate-convergence", "research-mgr"));
edges.push(edge("e-debate-convergence-trader", "debate-convergence", "trader"));
```

---

### H2: Rhai 共识因子 f2_weight 复用 "trend" 键

**位置**：`portfolio-mgr.rhai:42-48`

**问题**：

```rhai
// 因子 1: 技术面
let f1_weight = if present(totalScore) { get_weight(f_weights, "trend", f1_default) } else { 0.0 };

// 因子 2: 共识
let f2_weight = if present(consensusScore) { get_weight(f_weights, "trend", f2_default) } else { 0.0 };
//                                                                    ^^^^^^  应为 "consensus" 或 "debate"
```

因子1（技术面）和因子2（共识）都从 `factor_weights` 中取 `"trend"` 键的权重。`factor_weights` 来自 `t-scoring.result.factor_backtest.factors`，其结构为 `{trend:{weight,...}, macd:{...}, rsi:{...}, ...}`。

**影响**：

- 若回测数据中 `trend` 权重偏高/偏低，两个因子会同时被放大/缩小
- 共识因子本应有独立的回测权重（如辩论收敛信号的信息系数），但被技术面权重覆盖
- 当 `factor_weights` 缺失时，两者都用默认值 0.25，尚可接受；但回测数据存在时决策偏差

**修复**：

```rhai
let f2_weight = if present(consensusScore) { get_weight(f_weights, "consensus", f2_default) } else { 0.0 };
```

> 需同步在 `compute_scoring` 工具的回测逻辑中增加 `consensus` 因子的 IC 计算，或在 factor_weights 无此键时优雅 fallback 到 f2_default。

---

### H3: `evidence_pct` 分母硬编码为 1.0

**位置**：`portfolio-mgr.rhai:212`

**问题**：

```rhai
let evidence_pct = (total_weight / 1.0 * 100.0).to_int();
```

`total_weight` 是 6 个因子权重的实际总和。6 个因子的默认权重：

- f1_default = 0.25（技术面）
- f2_default = 0.25（共识）
- f3_default = 0.20（催化剂）
- f4_default = 0.15（风险）
- f5_default = 0.15（数据质量）
- f6_weight = 0.20（交易方案质量，固定值）

理论最大权重 = 0.25 + 0.25 + 0.20 + 0.15 + 0.15 + 0.20 = **1.20**

当所有因子都激活时，`evidence_pct = (1.20 / 1.0 * 100) = 120`，被 `.to_int()` 截断为 120（超出 100% 语义）。当部分因子缺失时，evidence_pct 会显示一个低于实际覆盖比例的值。

**影响**：`evidence_pct` 显示在 `reasoning` 字段中供用户参考，数值不准确会误导用户对"证据充分度"的判断。

**修复**：

```rhai
let max_weight = 0.25 + 0.25 + 0.20 + 0.15 + 0.15 + 0.20;  // 1.20
let evidence_pct = (total_weight / max_weight * 100.0).to_int();
let evidence_pct = if evidence_pct > 100 { 100 } else { evidence_pct };
```

---

## MEDIUM 级缺陷（已知但未根治）

### M1: 风险雷达图 fallback 关键词累加

**位置**：`src/components/stock-analysis/RiskMatrix.tsx:282-321`

**现状**：三级优先级已修复（VERDICT position_pct → confidence → fallback 关键词）。但 fallback 路径（VERDICT 缺失时）基准分 25 + 关键词累加（每次约 +6）仍可能逼近 100。

**影响**：生产环境 LLM 风险师正常输出都带 VERDICT JSON，fallback 几乎不触发。仅在 LLM 异常输出时可能误判。

**建议**：fallback 路径也加 `Math.min(75, score)` 封顶，避免极端情况。

---

### M2: LLM 失败级联

**位置**：`portfolio-mgr.rhai` 全文

**现状**：portfolio-mgr 是纯算法 CodeNode，依赖上游 LLM 节点（trader/debate-convergence/a-catalyst）输出。若 LLM 失败：

- `trader_target_price` / `trader_stop_loss` 缺失 → `f6_weight = 0`，`odds` 用默认 2.0
- `consensusScore` 缺失 → `f2_weight = 0`
- `catalyst_level` 缺失 → `f3_weight = 0`
- `posterior` 退化为 `prior + (f1+f4+f5 信号) * 0.35`

**现状评估**：已在 V29 部分修复（t-scoring 出边改为纯算法节点，insufficient_data 时 final_confidence/position_pct 归零）。但当多个 LLM 因子同时缺失时，`total_weight` 可能 < 0.5，`evidence_pct` 显示极低，`posterior` 主要由先验和算法因子决定，决策质量下降但不崩溃。

**建议**：在 Rhai 中增加 `total_weight < 0.3` 时强制 `position_pct = 0` 的兜底逻辑（当前仅靠 `posterior < 0.50` 间接实现）。

---

## 修复优先级建议

| 优先级    | 编号   | 预计工作量              | 风险                                        |
| --------- | ------ | ----------------------- | ------------------------------------------- |
| P0 立即修 | C1     | 改 3 行                 | 极低，路径修正不影响其他节点                |
| P0 立即修 | C3     | 改 1 行                 | 极低，路径修正                              |
| P0 立即修 | C2     | 改 20 行                | 极低，路径批量修正                          |
| P1 本周修 | C4     | 改 3 行 + 验证          | 中，需确认 risk-agg 等 AgentNode 输出字段名 |
| P1 本周修 | H1     | 改 2 处 context_sources | 低，加 source 不影响现有逻辑                |
| P2 近期修 | H2     | 改 1 行 + 回测逻辑      | 中，需同步 compute_scoring 工具             |
| P2 近期修 | H3     | 改 2 行                 | 极低                                        |
| P3 后续修 | M1, M2 | 各几行                  | 低                                          |

---

## 附录：节点输出结构速查表

| 节点类型              | 输出结构                                                                           | 正确路径示例                          |
| --------------------- | ---------------------------------------------------------------------------------- | ------------------------------------- |
| **ToolNode**          | `{tool_name, result: <json_string>, truncated, node_id}`                           | `t-scoring.result.totalScore`         |
| **AgentNode**         | `{role, model, content: <json_string>, thinking, usage, tool_calls_made, node_id}` | `a-market-analyst.content.confidence` |
| **LlmClassifierNode** | `{category, model, provider, input_var, node_id}`                                  | `risk-level.category`                 |
| **AggregatorNode**    | `{strategy, source_count, wait_for_all, result: <aggregated>, node_id}`            | result 结构取决于 strategy            |
| **CodeNode**          | `{status, result: <rhai_output>, input_params, node_id, params}`                   | `portfolio-mgr.result.action`         |

> **关键规则**：`resolve_var_path` 遇到 `Value::String` 会自动 `from_str` 解析为 JSON 再继续下钻。因此 ToolNode 的 `result`（JSON 字符串）和 AgentNode 的 `content`（JSON 字符串）都能通过 `.result.field` / `.content.field` 路径访问内部字段。
