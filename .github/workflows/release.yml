name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Git tag to release (e.g., v1.5.9)'
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  actions: write

env:
  CARGO_INCREMENTAL: 0
  RUST_BACKTRACE: short
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  NDK_VERSION: 'r26d'

concurrency:
  group: '${{ github.workflow }}-${{ github.ref }}'
  cancel-in-progress: true

jobs:
  # ── Resolve tag name for both tag push and manual dispatch ──
  resolve-tag:
    name: Resolve Tag Name
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.out.outputs.value }}
    steps:
      - id: out
        run: |
          TAG="${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}"
          echo "value=$TAG" >> "$GITHUB_OUTPUT"
          echo "::notice::Resolved tag: $TAG"

  # ── Validate tag matches package.json version ──────────
  check-version:
    name: Check Version
    needs: [resolve-tag]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: master

      - name: Validate tag vs package.json
        run: |
          TAG="${{ needs.resolve-tag.outputs.tag }}"
          # 从 tag 中提取纯净版号: v2.5.0-beta.1 → 2.5.0
          CLEAN_TAG=$(echo "$TAG" | sed 's/^v//' | sed 's/-.*//')
          PKG_VERSION=$(jq -r .version package.json)
          if [[ "$CLEAN_TAG" != "$PKG_VERSION" ]]; then
            echo "❌ Tag $TAG (clean: $CLEAN_TAG) does not match package.json $PKG_VERSION"
            exit 1
          fi
          echo "✅ Version $TAG is consistent with package.json $PKG_VERSION"

  # ── Generate release notes via git-cliff ─────────────
  draft-release-notes:
    name: Generate Release Notes
    needs: [resolve-tag, check-version]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      body: ${{ steps.cliff.outputs.content }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Generate changelog
        uses: orhun/git-cliff-action@v4
        id: cliff
        with:
          config: cliff.toml
          args: --latest --strip header
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPO: ${{ github.repository }}
          CLIFF_REPO: ${{ github.repository }}

      - name: Create draft release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.resolve-tag.outputs.tag }}
          name: 'AxInvest ${{ needs.resolve-tag.outputs.tag }}'
          body: ${{ steps.cliff.outputs.content }}
          draft: true
          prerelease: ${{ contains(needs.resolve-tag.outputs.tag, '-rc') || contains(needs.resolve-tag.outputs.tag, '-beta') || contains(needs.resolve-tag.outputs.tag, '-alpha') }}
          token: ${{ secrets.GITHUB_TOKEN }}

  # ── Build for macOS / Windows / Linux x86_64 ───────────
  release:
    name: Build (${{ matrix.target }})
    needs: [resolve-tag, check-version, draft-release-notes]
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: macos-latest
            target: x86_64-apple-darwin
          # Windows
          - os: windows-latest
            target: x86_64-pc-windows-msvc
          - os: windows-latest
            target: aarch64-pc-windows-msvc
          # Linux x86_64 (native build on x64 runner; 不需要 cross 工具链)
          # 注：必须用 ubuntu-24.04 (noble)，其 libpipewire-0.3-dev = 1.0.5，
          # 否则 libspa-0.9.2 safe wrapper 与 22.04 的 pipewire 0.3.48 C 结构体不匹配
          # (spa_video_info_raw 缺 flags 字段、modifier 为 i64 而非 u64)
          - os: ubuntu-24.04
            target: x86_64-unknown-linux-gnu

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: master
          submodules: recursive

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.97.0"
          targets: ${{ matrix.target }}

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: 'v1'
          key: '${{ matrix.os }}-${{ matrix.target }}'
          workspaces: src-tauri -> target
          cache-targets: ${{ matrix.os != 'windows-latest' }}

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-24.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libpipewire-0.3-dev \
            libgbm-dev

      - name: Install Protoc (Ubuntu)
        if: matrix.os == 'ubuntu-24.04'
        run: sudo apt-get install -y protobuf-compiler

      - name: Install x86_64 OpenSSL (macOS Intel cross-compile)
        if: matrix.target == 'x86_64-apple-darwin'
        run: |
          brew install openssl@3
          echo "OPENSSL_DIR=$(brew --prefix openssl@3)" >> $GITHUB_ENV
          echo "OPENSSL_INCLUDE_DIR=$(brew --prefix openssl@3)/include" >> $GITHUB_ENV
          echo "OPENSSL_LIB_DIR=$(brew --prefix openssl@3)/lib" >> $GITHUB_ENV

      - name: Install Protoc (macOS)
        if: matrix.os == 'macos-latest'
        run: brew install protobuf

      - name: Install create-dmg (macOS DMG bundling)
        if: matrix.os == 'macos-latest'
        run: brew install create-dmg

      - name: Install Protoc (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          $ver = '27.3'
          Invoke-WebRequest -Uri "https://github.com/protocolbuffers/protobuf/releases/download/v$ver/protoc-$ver-win64.zip" -OutFile protoc.zip
          Expand-Archive protoc.zip -DestinationPath "$env:RUNNER_TOOL_CACHE\protoc"
          echo "$env:RUNNER_TOOL_CACHE\protoc\bin" | Out-File -FilePath $env:GITHUB_PATH -Append

      - uses: actions/setup-node@v5
        with:
          node-version: 24

      - name: Install frontend dependencies
        run: npm install

      - name: Build with Tauri
        uses: tauri-apps/tauri-action@v0.5
        env:
          NODE_OPTIONS: '--max_old_space_size=4096'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          APPLE_SIGNING_IDENTITY: ${{ matrix.os == 'macos-latest' && '-' || '' }}
        with:
          # NOTE: Do NOT set tagName/releaseDraft here — tauri-action manages
          # releases via softprops/action-gh-release internally, and running
          # 6+ parallel builds all creating/updating the same draft release
          # causes duplicate-draft race conditions that lose artifacts.
          # The draft is created once by the `draft-release-notes` job, and
          # artifacts are uploaded by explicit softprops/action-gh-release steps below.
          tauriScript: npm run tauri
          args: --target ${{ matrix.target }}
          includeUpdaterJson: true

      - name: Package Windows artifacts
        if: contains(matrix.target, 'windows')
        shell: pwsh
        run: |
          $target = "${{ matrix.target }}"
          $version = "${{ needs.resolve-tag.outputs.tag }}"
          $releaseDir = Join-Path "src-tauri/target" (Join-Path $target "release")
          $arch = if ($target -match "aarch64") { "arm64" } else { "x64" }

          # 主二进制名解析（顺序即优先级）：
          #   1. AxInvest.exe — tauri.conf.json 的 mainBinaryName 生效时由 tauri CLI 重命名产出
          #   2. axagent.exe  — cargo [[bin]] name，未设置 mainBinaryName 时的原始产物
          #   3. AxAgent.exe  — productName（历史/其他配置下的可能名）
          # 三者都缺失时，退化为扫描 release 目录下体积最大的 .exe，并打印目录便于排查
          $exe = @("AxInvest.exe", "axagent.exe", "AxAgent.exe") |
            ForEach-Object { Join-Path $releaseDir $_ } |
            Where-Object { Test-Path $_ } |
            Select-Object -First 1

          if (-not $exe) {
            $candidate = Get-ChildItem -Path $releaseDir -Filter "*.exe" -File -ErrorAction SilentlyContinue |
              Where-Object { $_.Length -gt 1MB } |
              Sort-Object Length -Descending |
              Select-Object -First 1
            if (-not $candidate) {
              Write-Error "No built executable found under: $releaseDir"
              Get-ChildItem -Path $releaseDir -ErrorAction SilentlyContinue | Format-Table Name, Length
              exit 1
            }
            $exe = $candidate.FullName
          }

          Write-Host "Packaging $exe ($([math]::Round((Get-Item $exe).Length / 1MB, 1)) MB)"

          # portable 压缩包：统一以 AxInvest.exe 交付，解压即可运行
          $staging = "portable-staging-$arch"
          $staged = Join-Path $staging "AxInvest.exe"
          New-Item -ItemType Directory -Path $staging -Force | Out-Null
          Copy-Item -Path $exe -Destination $staged -Force
          $zipName = "AxInvest_${version}_windows-${arch}-portable.zip"
          Compress-Archive -Path $staged -DestinationPath $zipName
          Remove-Item -Recurse -Force $staging

          $msiPath = Join-Path $releaseDir (Join-Path "bundle" "msi")
          if (Test-Path $msiPath) {
            $msiFiles = Get-ChildItem -Path $msiPath -Filter "*.msi" -File
            if ($msiFiles.Count -eq 0) {
              Write-Warning "MSI directory exists but contains no .msi: $msiPath"
            }
            foreach ($msi in $msiFiles) {
              $newMsiName = "AxInvest_${version}_windows-${arch}.msi"
              Move-Item -Path $msi.FullName -Destination (Join-Path $releaseDir $newMsiName) -Force
            }
          } else {
            Write-Warning "MSI bundle directory not found: $msiPath (aarch64 cross-build may skip WiX)"
          }

      - name: Package Linux x86_64 artifacts
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        run: |
          if [ -d "src-tauri/target/x86_64-unknown-linux-gnu/release/bundle/deb" ]; then
            cd src-tauri/target/x86_64-unknown-linux-gnu/release/bundle/deb
            for deb in *.deb; do
              new_name="AxInvest_${{ needs.resolve-tag.outputs.tag }}_linux-x64.deb"
              mv "$deb" "$new_name"
              mv "$new_name" "../../$new_name"
            done
          fi

          if [ -d "src-tauri/target/x86_64-unknown-linux-gnu/release/bundle/rpm" ]; then
            cd src-tauri/target/x86_64-unknown-linux-gnu/release/bundle/rpm
            for rpm in *.rpm; do
              new_name="AxInvest_${{ needs.resolve-tag.outputs.tag }}_linux-x64.rpm"
              mv "$rpm" "$new_name"
              mv "$new_name" "../../$new_name"
            done
          fi

      - name: Upload Windows artifacts to release
        if: contains(matrix.target, 'windows')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.resolve-tag.outputs.tag }}
          draft: true
          token: ${{ secrets.GITHUB_TOKEN }}
          files: |
            AxInvest_${{ needs.resolve-tag.outputs.tag }}_windows-*-portable.zip
            src-tauri/target/${{ matrix.target }}/release/AxInvest_${{ needs.resolve-tag.outputs.tag }}_windows-*.msi

      - name: Package macOS artifacts
        if: contains(matrix.target, 'apple-darwin')
        run: |
          cd src-tauri/target/${{ matrix.target }}/release/bundle/dmg
          for dmg in *.dmg; do
            if [[ "${{ matrix.target }}" == "aarch64-apple-darwin" ]]; then
              new_name="AxInvest_${{ needs.resolve-tag.outputs.tag }}_macos-arm64.dmg"
            else
              new_name="AxInvest_${{ needs.resolve-tag.outputs.tag }}_macos-x64.dmg"
            fi
            mv "$dmg" "$new_name"
            mv "$new_name" "../../$new_name"
          done

      - name: Upload Linux x86_64 artifacts
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.resolve-tag.outputs.tag }}
          draft: true
          token: ${{ secrets.GITHUB_TOKEN }}
          files: |
            src-tauri/target/x86_64-unknown-linux-gnu/release/AxInvest_${{ needs.resolve-tag.outputs.tag }}_linux-x64.deb
            src-tauri/target/x86_64-unknown-linux-gnu/release/AxInvest_${{ needs.resolve-tag.outputs.tag }}_linux-x64.rpm

      - name: Upload macOS artifacts
        if: contains(matrix.target, 'apple-darwin')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.resolve-tag.outputs.tag }}
          draft: true
          token: ${{ secrets.GITHUB_TOKEN }}
          files: |
            src-tauri/target/${{ matrix.target }}/release/AxInvest_${{ needs.resolve-tag.outputs.tag }}_macos-*.dmg

  # ── Build for Linux ARM64 (native on ARM64 runner) ───
  # 注：用 GitHub ARM64 native runner (ubuntu-24.04-arm) 原生编译，
  # host = target = aarch64，无 cross-compile，彻底绕过 rust-lld bug。
  # rustc 1.67+ 在 x86_64 cross-compile aarch64 时，rust-lld 错误地按
  # target (aarch64) 架构链接 host (x86_64) proc-macro，导致：
  #   rust-lld: error: .../x86_64-linux-gnu/crti.o is incompatible with elf64-littleaarch64
  # 前 9 次尝试用 RUSTFLAGS/env var/ld.lld wrapper 修都失败（rustc 强制注入
  # -fuse-ld=lld -B<sysroot>/gcc-ld/）。native ARM64 runner 是唯一可靠方案。
  # GitHub 2025-01-16 推出 ARM64 runner，对 public repo 免费。
  #
  release-linux-arm:
    name: Build (aarch64-unknown-linux-gnu)
    needs: [resolve-tag, check-version, draft-release-notes]
    runs-on: ubuntu-24.04-arm
    env:
      # ARM64 runner 内存较小 (~4GB)，限制并行度避免 OOM
      CARGO_BUILD_JOBS: 1
      # 关闭 LTO 降链接阶段内存峰值（ARM64 runner 内存不足）
      # 注意：此环境变量会覆盖 Cargo.toml 的 lto = true
      CARGO_PROFILE_RELEASE_LTO: off
      # 覆盖 Cargo.toml 的 codegen-units=1（单 rustc 峰值最大），
      # 提高 codegen 单元数降低单 rustc 编译峰值内存（2026-08-09 补充）
      CARGO_PROFILE_RELEASE_CODEGEN_UNITS: 16
    steps:
      - uses: actions/checkout@v5
        with:
          ref: master
          submodules: recursive

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.97.0"
          targets: aarch64-unknown-linux-gnu

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: 'v1'
          key: 'ubuntu-24.04-arm-aarch64-unknown-linux-gnu'
          workspaces: src-tauri -> target

      - name: Install ARM64 native dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            protobuf-compiler \
            patchelf \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libpipewire-0.3-dev \
            libgbm-dev \
            libxkbcommon-dev \
            libpango1.0-dev \
            libatk-bridge2.0-dev \
            libatk1.0-dev \
            libcups2-dev

      - uses: actions/setup-node@v5
        with:
          node-version: 24

      - name: Install frontend dependencies
        run: npm install

      - name: Build for Linux ARM64
        run: |
          npm run tauri build -- --target aarch64-unknown-linux-gnu --bundles deb,rpm
        env:
          NODE_OPTIONS: '--max_old_space_size=4096'
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Package Linux ARM64 artifacts
        run: |
          if [ -d "src-tauri/target/aarch64-unknown-linux-gnu/release/bundle/deb" ]; then
            cd src-tauri/target/aarch64-unknown-linux-gnu/release/bundle/deb
            for deb in *.deb; do
              new_name="AxInvest_${{ needs.resolve-tag.outputs.tag }}_linux-arm64.deb"
              mv "$deb" "$new_name"
              mv "$new_name" "../../$new_name"
            done
          fi

          if [ -d "src-tauri/target/aarch64-unknown-linux-gnu/release/bundle/rpm" ]; then
            cd src-tauri/target/aarch64-unknown-linux-gnu/release/bundle/rpm
            for rpm in *.rpm; do
              new_name="AxInvest_${{ needs.resolve-tag.outputs.tag }}_linux-arm64.rpm"
              mv "$rpm" "$new_name"
              mv "$new_name" "../../$new_name"
            done
          fi

      - name: Upload Linux ARM64 artifacts
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.resolve-tag.outputs.tag }}
          name: 'AxInvest ${{ needs.resolve-tag.outputs.tag }}'
          draft: true
          prerelease: ${{ contains(needs.resolve-tag.outputs.tag, '-rc') || contains(needs.resolve-tag.outputs.tag, '-beta') || contains(needs.resolve-tag.outputs.tag, '-alpha') }}
          token: ${{ secrets.GITHUB_TOKEN }}
          files: |
            src-tauri/target/aarch64-unknown-linux-gnu/release/AxInvest_${{ needs.resolve-tag.outputs.tag }}_linux-arm64.deb
            src-tauri/target/aarch64-unknown-linux-gnu/release/AxInvest_${{ needs.resolve-tag.outputs.tag }}_linux-arm64.rpm

  build-android:
    name: Build Android (${{ matrix.abi }})
    needs: [resolve-tag, draft-release-notes]
    strategy:
      fail-fast: false
      matrix:
        abi: [arm64-v8a, armeabi-v7a, x86_64]
    runs-on: ubuntu-24.04
    env:
      # Android 构建也容易 OOM，限制并行度并关闭 LTO
      CARGO_BUILD_JOBS: 1
      # 关闭 LTO 降链接阶段内存峰值
      # 注意：此环境变量会覆盖 Cargo.toml 的 lto = true
      CARGO_PROFILE_RELEASE_LTO: off
      # 覆盖 Cargo.toml 的 codegen-units=1，降低单 rustc 编译峰值内存（2026-08-09 补充）
      CARGO_PROFILE_RELEASE_CODEGEN_UNITS: 16
    steps:
      - uses: actions/checkout@v5
        with:
          ref: master
          submodules: recursive

      - name: Free disk space
        run: |
          df -h /
          sudo rm -rf /usr/share/dotnet /opt/ghc \
            /opt/hostedtoolcache/CodeQL /usr/local/share/powershell 2>/dev/null || true
          sudo docker image prune -af 2>/dev/null || true
          # 清理 Android 临时文件
          sudo rm -rf /tmp/android* /tmp/gradle* /tmp/build* 2>/dev/null || true
          # 清理 Rust Cargo 缓存
          rm -rf ~/.cargo/registry/cache/* 2>/dev/null || true
          rm -rf ~/.cargo/git/db/* 2>/dev/null || true
          df -h /

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.97.0"
          targets: ${{ matrix.abi == 'arm64-v8a' && 'aarch64-linux-android' || matrix.abi == 'armeabi-v7a' && 'armv7-linux-androideabi' || 'x86_64-linux-android' }}

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: 'v2-android'
          key: 'ubuntu-24.04-ndk${{ env.NDK_VERSION }}-${{ matrix.abi }}'
          workspaces: src-tauri -> target

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24

      - name: Install frontend dependencies
        run: npm install

      - name: Install Protoc
        uses: arduino/setup-protoc@v3
        with:
          version: '27.x'
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Android NDK
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: ${{ env.NDK_VERSION }}
          link-to-sdk: true

      - name: Setup JDK 17 (Zulu)
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          api-level: 34
          target-sdk: 34
          build-tools: '34.0.0'
          cmake: '3.22.1'

      - name: Verify NDK installation
        run: |
          NDK_PATH="${{ steps.setup-ndk.outputs.ndk-path }}"
          echo "NDK path: $NDK_PATH"
          ls -la "$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64/bin/" | head -20
          echo "---"
          # 检查关键工具是否存在
          for tool in aarch64-linux-android-clang armv7a-linux-androideabi-clang x86_64-linux-android-clang; do
            if [ -f "$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64/bin/$tool" ]; then
              echo "✅ $tool found"
            else
              echo "❌ $tool NOT found"
            fi
          done
          echo "---"
          # 检查 NDK 平台支持
          ls "$NDK_PATH/platforms/" | head -10
          echo "---"
          echo "Available ABIs:"
          ls "$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64/lib64/clang/*/lib/linux/" 2>/dev/null || true

      - name: Install cargo-ndk
        run: cargo install cargo-ndk --version 4.1.2

      - name: Setup Android NDK toolchain PATH
        run: |
          NDK_PATH="${{ steps.setup-ndk.outputs.ndk-path }}"
          TOOLCHAIN="$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64/bin"
          echo "$TOOLCHAIN" >> $GITHUB_PATH
          echo "NDK_PATH=$NDK_PATH" >> $GITHUB_ENV
          echo "ANDROID_NDK_HOME=$NDK_PATH" >> $GITHUB_ENV
          echo "NDK_HOME=$NDK_PATH" >> $GITHUB_ENV
          echo "=== NDK Setup ==="
          echo "NDK_PATH: $NDK_PATH"
          ls -la "$NDK_PATH/" 2>/dev/null || echo "ERROR: NDK path not found!"
          ls -la "$TOOLCHAIN/" 2>/dev/null || echo "ERROR: Toolchain not found!"
          echo "=================="

      - name: Initialize Android project
        run: |
          cd src-tauri
          echo "=== Checking Android SDK ==="
          echo "ANDROID_HOME: $ANDROID_HOME"
          echo "ANDROID_NDK_HOME: $ANDROID_NDK_HOME"
          echo "NDK_HOME: $NDK_HOME"
          ls -la "$ANDROID_HOME" 2>/dev/null || echo "ANDROID_HOME not set"
          ls -la "$NDK_HOME" 2>/dev/null || echo "NDK_HOME not set"
          echo "==============================="
          npx tauri android init
          echo "=== Generated Android project ==="
          ls -la gen/android/
          echo "==============================="
        env:
          NDK_HOME: ${{ env.NDK_HOME }}
          ANDROID_NDK_HOME: ${{ env.NDK_HOME }}

      - name: Ensure Gradle Wrapper
        run: |
          cd src-tauri/gen/android
          if [ ! -f "gradlew" ] || [ ! -f "gradle/wrapper/gradle-wrapper.jar" ]; then
            echo "Gradle wrapper missing, generating..."
            gradle wrapper --gradle-version 8.9
            chmod +x gradlew
          fi
          echo "=== Gradle Wrapper Status ==="
          ls -la gradlew
          ls -la gradle/wrapper/gradle-wrapper.jar
          cat gradle/wrapper/gradle-wrapper.properties
          echo "==============================="
          # Verify critical files exist
          if [ ! -f "gradlew" ]; then
            echo "ERROR: gradlew not found"
            exit 1
          fi
          if [ ! -f "gradle/wrapper/gradle-wrapper.jar" ]; then
            echo "ERROR: gradle-wrapper.jar not found"
            exit 1
          fi

      - name: Setup keystore and signing config
        run: |
          mkdir -p src-tauri/gen/android/app
          echo "${{ secrets.KEYSTORE_FILE }}" | base64 -d > src-tauri/gen/android/app/release.jks
          printf 'storeFile=release.jks\nstorePassword=%s\nkeyAlias=%s\nkeyPassword=%s\n' \
            "${{ secrets.KEYSTORE_PASSWORD }}" "${{ secrets.KEY_ALIAS }}" "${{ secrets.KEY_PASSWORD }}" \
            > src-tauri/gen/android/keystore.properties
          python3 scripts/patch-android-signing.py src-tauri/gen/android/app/build.gradle.kts
          # 添加 ProGuard 规则，防止 R8 混淆/剥离 JNI 桥接类导致闪退
          cp src-tauri/proguard-rules.pro src-tauri/gen/android/app/proguard-rules.pro

      - name: Build frontend (Vite)
        run: npm run build:skip-typecheck

      - name: Limit Gradle memory (prevent runner OOM)
        run: |
          GP=src-tauri/gen/android/gradle.properties
          if [ ! -f "$GP" ]; then
            echo "gradle.properties not found, skip memory tuning"
            exit 0
          fi
          # tauri android init 生成的 gradle.properties 末行（如 android.nonFinalResIds=false）
          # 没有结尾换行符。若不先补上，后续 append 的配置会与末行合并到同一行，
          # 导致 Gradle 解析失败：Cannot parse project property ... as boolean.
          if [ -s "$GP" ] && [ -n "$(tail -c 1 "$GP")" ]; then
            echo "" >> "$GP"
          fi
          # ARM 交叉编译是内存大户：cargo 交叉编译 + Gradle 共存极易吃光 7GB runner
          # 统一策略：Gradle JVM 收紧、禁用并行、workers=2
          if [ "${{ matrix.abi }}" = "armeabi-v7a" ] || [ "${{ matrix.abi }}" = "arm64-v8a" ]; then
            sed -i '/^org.gradle.jvmargs=/d; /^kotlin.daemon.jvm.options=/d; /^org.gradle.parallel=/d; /^org.gradle.workers.max=/d' "$GP"
            printf '%s\n' \
              'org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=768m -XX:+UseParallelGC' \
              'kotlin.daemon.jvm.options=-Xmx768m' \
              'org.gradle.parallel=false' \
              'org.gradle.workers.max=2' >> "$GP"
          else
            grep -q '^org.gradle.jvmargs=' "$GP" \
              || echo 'org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=1024m -XX:+UseParallelGC' >> "$GP"
          fi
          echo "--- gradle.properties ---"
          cat "$GP"

      # 注：APK 与 AAB 在同一次 `tauri android build` 中产出（--apk --aab），
      # 避免为 AAB 单独再启动一次完整的 gradle 构建。
      # --target 参数接受: aarch64, armv7, i686, x86_64
      # 内存策略：
      #   1) CARGO_PROFILE env：Cargo 1.78+ 原生支持，覆盖默认 profile，
      #      release-android 关闭 LTO + codegen-units=16，避免 ARM 交叉编译 OOM。
      #   2) tauri android build 内部通过 Gradle 调用 cargo，CARGO_PROFILE 会被 cargo 读取。
      - name: Build Android APK + AAB
        timeout-minutes: 240
        run: |
          cd src-tauri
          TARGET=${{ matrix.abi == 'arm64-v8a' && 'aarch64' || matrix.abi == 'armeabi-v7a' && 'armv7' || 'x86_64' }}
          # ARM 交叉编译：收紧 cargo 并发 + 使用 release-android profile 降内存
          # x86_64 是 native 编译，仍用 release profile（速度/体积优先）
          if [ "${{ matrix.abi }}" = "armeabi-v7a" ] || [ "${{ matrix.abi }}" = "arm64-v8a" ]; then
            export CARGO_BUILD_JOBS=1
            export CARGO_PROFILE=release-android
          else
            export CARGO_BUILD_JOBS=2
            export CARGO_PROFILE=release
          fi
          echo "=== System Resources Before Build ==="
          echo "Memory:"
          free -h
          echo "Disk:"
          df -h /
          echo "Gradle version:"
          gradle --version 2>/dev/null || echo "gradle CLI not available"
          echo "Java version:"
          java -version 2>&1
          echo "NDK path: $NDK_HOME"
          echo "Cargo profile: $CARGO_PROFILE"
          echo "Cargo jobs: $CARGO_BUILD_JOBS"
          ls "$NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/" 2>/dev/null | head -5 || echo "WARNING: NDK toolchain not found at $NDK_HOME"
          echo "======================================="
          echo "=== Running tauri android build ==="
          if [[ "${{ matrix.abi }}" == "arm64-v8a" ]]; then
            npx tauri android build --target "$TARGET" --apk --aab
          else
            npx tauri android build --target "$TARGET" --apk
          fi
        env:
          NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
          ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # 不再通过 env 覆盖 JVM args，统一由 gradle.properties 管理，避免冲突
          RUST_BACKTRACE: full

      - name: Upload APK and AAB to release
        run: |
          APK=$(find src-tauri/gen/android/app/build/outputs/apk/release -name "*.apk" -type f 2>/dev/null | head -1)
          if [ -z "$APK" ]; then
            APK=$(find src-tauri/gen/android/app/build/outputs/apk -path "*/release/*" -name "*.apk" -type f 2>/dev/null | head -1)
          fi
          
          if [ -n "$APK" ]; then
            ABI="${{ matrix.abi }}"
            NEW_NAME="AxInvest_${{ needs.resolve-tag.outputs.tag }}_android-${ABI}.apk"
            cp "$APK" "$GITHUB_WORKSPACE/$NEW_NAME"
            echo "Uploading APK: $NEW_NAME"
            cd "$GITHUB_WORKSPACE"
            gh release upload "${{ needs.resolve-tag.outputs.tag }}" "$NEW_NAME" --clobber
          else
            echo "APK not found"
            find src-tauri/gen/android/app/build/outputs/apk -name "*.apk" -type f 2>/dev/null || true
            exit 1
          fi
          
          if [ "${{ matrix.abi }}" = "arm64-v8a" ]; then
            AAB=$(find src-tauri/gen/android/app/build/outputs/bundle -name "*.aab" -type f 2>/dev/null | head -1)
            if [ -n "$AAB" ]; then
              AAB_NAME="AxInvest_${{ needs.resolve-tag.outputs.tag }}_android-play-store.aab"
              cp "$AAB" "$GITHUB_WORKSPACE/$AAB_NAME"
              echo "Uploading AAB: $AAB_NAME"
              cd "$GITHUB_WORKSPACE"
              gh release upload "${{ needs.resolve-tag.outputs.tag }}" "$AAB_NAME" --clobber
            else
              echo "AAB not found, uploading APK only"
            fi
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ── Build for iOS (暂时禁用：需要 Mac 环境和 Apple 签名证书) ──
  # TODO: 获取 Apple Developer ID 证书 + 在 Mac 上初始化 tauri ios 项目后恢复
  # build-ios:
  #   name: Build iOS
  #   needs: [resolve-tag, draft-release-notes]
  #   runs-on: macos-latest
  #   steps:
  #     ... (详见历史版本)

  # ── Publish draft release as latest ────────────────────
  publish-release:
    name: Publish Release
    needs: [resolve-tag, release, release-linux-arm, build-android]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Publish draft release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.resolve-tag.outputs.tag }}
          draft: false
          prerelease: ${{ contains(needs.resolve-tag.outputs.tag, '-rc') || contains(needs.resolve-tag.outputs.tag, '-beta') || contains(needs.resolve-tag.outputs.tag, '-alpha') }}
          make_latest: true
          token: ${{ secrets.GITHUB_TOKEN }}

  # ── Deploy website after release ────────────────────────
  deploy-website:
    name: Deploy Website
    needs: [publish-release]
    runs-on: ubuntu-latest
    steps:
      - name: Trigger website deployment
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'deploy-website.yml',
              ref: 'master',
            });
