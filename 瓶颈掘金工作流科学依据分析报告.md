# 瓶颈掘金工作流科学依据全面分析报告

> 分析时间：2026-06-27 | 覆盖范围：`stock_analysis_setup.rs` / `stock_workflow.rs` / 4 个专家 prompt / `serenity.rs` / `portfolio-manager.md`

---

## 问题：为什么瓶颈掘金候选股被判定"高风险+观望"？

瓶颈掘金（Serenity-screening）工作流筛选出的候选股票，在后续的股票分析 DAG 中常常得到"高风险"和"观望"决策。这看起来像是两个系统在"打架"——一个说好，一个说不好。

**核心结论：这不是矛盾，而是两个系统之间存在结构性脱节。**

---

## 一、瓶颈掘金工作流架构速览

### 5 阶段多 Agent 流水线

```
Phase 0 数据采集 ──→ Phase 0b 趋势扫描 ──→ Phase 1 瓶颈分析 ──→ Phase 2 候选映射 ──→ Phase 3 持久化
(t-industry-rank)   (trend-scanner)       (chain-decomposer ×3   (candidate-mapper)   (StorageNode +
(t-cls-flash)                               + chokepoint-                           reco_picks表)
(t-northbound)                               identifier ×3)
```

### 4 个 LLM Agent 各司其职

| Agent                     | 职责                                                | 输出                                                     |
| ------------------------- | --------------------------------------------------- | -------------------------------------------------------- |
| **trend-scanner**         | 从市场噪声中识别"萌芽→加速"阶段的产业方向           | 2-3 个趋势（含置信度、因果链、需求证据）                 |
| **chain-decomposer**      | 将一个趋势拆解为 5-8 个供应链环节，标注壁垒和集中度 | 供应链图谱（供应商数量、技术壁垒、扩产周期）             |
| **chokepoint-identifier** | 三力评分验证哪个环节是真正的瓶颈                    | composite_score（供给刚性+需求弹性+不可替代性）          |
| **candidate-mapper**      | 将瓶颈映射到 A 股投资标的，输出候选清单             | 3-5 个候选股（serenity_score、催化剂、退出信号、关注度） |

### 财务验证层（SerenityStrategy）

瓶颈掘金产出的候选股通过静态全局变量 → SerenityStrategy（`strategies/serenity.rs`），执行：

- **硬门槛**：毛利率 ≥ 50%（高评分可降至 35%）、负债率 ≤ 60%（高评分可至 70%）、营收增速 ≥ 10%（高评分可至 5%）
- **置信度因子**：毛利率 25% + 营收增速 20% + ROE 15% + OCF 10% + 毛利率趋势 10% + workflow 评分 15% + 催化剂 5%

---

## 二、股票分析 DAG 的决策机制

### Portfolio Manager 的公式化决策

```
confidence = clamp(compute_scoring.totalScore + adjustment, 0, 100)

adjustment =
  + (consensus_score - 50) / 10          // 辩论收敛度
  + (dqi_data_quality - 50) / 5          // 数据质量
  + risk_adjustment                       // 风险等级偏移
  + catalyst_bonus + institutional_bonus  // 催化剂+机构加成

confidence < 50 → riskLevel = "高风险"
confidence < 40 → 噪音信号 → 建议"观望/持有"
```

### Quality Fallback 备降路径

```
data-quality-inspector 评分 D/F
 → quality-gate switch 走 default 分支
 → quality-fallback 输出: action ∈ {持有,减持,卖出}, positionPct ∈ [0,20]
```

---

## 三、脱节的核心：两个系统之间的"柏林墙"

```
瓶颈掘金工作流                         股票分析 DAG
(独立执行, 手动触发)                  (独立执行)
     │                                      │
     │  set_serenity_seed()                  │
     └──────→ 全局静态变量 ─────→ SerenityStrategy(仅荐股使用)
                                      不是 → 股票分析 DAG 的 PM 决策
```

**关键发现：PM 公式中没有任何 serenity_score、瓶颈评分或相关权重因子。** PM 不知道当前分析的股票是一个经过供应链瓶颈验证的候选股。

---

## 四、结构性矛盾：瓶颈掘金的"偏好"天然与 DAG 冲突

| 瓶颈掘金偏好               | 股票分析 DAG 依赖              | 冲突后果                   |
| -------------------------- | ------------------------------ | -------------------------- |
| 市值 50-500 亿，小盘股     | 数据越多越丰富越好             | 小市值→数据源少→dqi 评分低 |
| 机构覆盖少（<5篇研报/月）  | 多视角交叉验证                 | 覆盖少→共识难以形成        |
| 低关注度，越冷门越好       | 高置信度需要充足证据           | 低关注→confidence 被打压   |
| 产业链逻辑驱动（底层逻辑） | 技术面+资金面+估值（表层信号） | 逻辑层面完全错位           |

**这条因果链是自洽的：**

瓶颈掘金故意找"低关注"股票 → 低关注导致数据质量评分偏低 → PM 公式中 dqi 因子拉低 totalScore → final_confidence 偏低 → `confidence < 50` 触发高风险 → `< 40` 触发观望

如果 data-quality-inspector 输出 D/F → 直接被 quality-gate 拦截 → quality-fallback 输出"持有/观望"

---

## [追加发现] 比结构性脱节更严重的问题：数据源本身就是涨停板池

> 用户反馈：瓶颈掘金的候选股票都是已经涨停的股票。追查发现，Phase 0 的 `t-hot-stocks` 工具节点调用 `get_hot_stocks()`，其同花顺 vendor 的 API 地址是 `data.10jqka.com.cn/dataapi/limit_up/limit_up_pool`——**涨停板池 API**。

### 三个 vendor 全部偏向已上涨股票

| Vendor                    | 实际数据来源             | 返回什么            |
| ------------------------- | ------------------------ | ------------------- |
| **ths（同花顺）** ✅ 首选 | `limit_up/limit_up_pool` | **当日涨停/强势股** |
| **iwencai（问财）** 备选  | 查询"今日涨幅前20的股票" | 按涨幅排序          |
| **baidu_stock** 备选      | `&type=hot`              | 热门榜单            |

### 自我强化的闭环

```
t-hot-stocks → limit_up_pool API → 20只涨停/强势股
     ↓
trend-scanner 从涨停股中归纳 2-3 个趋势
     ↓
chokepoint-identifier 给这些赛道打分（因为它们确实"热"）
     ↓
candidate-mapper 从这些赛道找候选 → 还是这些涨停股
     ↓
输出全是涨停股——但瓶颈掘金的定位是"抢先发现未定价的瓶颈"
```

### 这是从根本上违背了瓶颈掘金的设计初衷

瓶颈掘金的目标是"找即将卡脖子而非已经卡脖子"的股票。但 **Phase 0 喂给它的是当日涨停股的趋势**，这让整个工作流变成了"涨停股归因分析"——从涨停股反推它们为什么涨停，再输出它们作为候选——完全是自我验证的循环。

### 已修复（2026-06-27）

**移除 `t-hot-stocks` 节点**，Phase 0 改为只采集行业级数据：

- `t-industry-rank`：行业涨跌排名（行业级信号）
- `t-cls-flash`：财联社实时快讯（新闻信号）
- `t-northbound`：北向资金流向（资金信号）

trend-scanner 的 prompt 也同步更新，不再依赖个股热度数据。

---

## 五、瓶颈掘金方法论科学依据评分

### ✅ 合理之处（4/8 维）

#### 1. 三力框架有产业经济学基础 ⭐⭐⭐⭐

供给刚性 + 需求弹性 + 不可替代性的三重验证框架，源自产业组织理论（Industrial Organization），在半导体/新能源/军工等供应链分析中被广泛使用。与 Porter 五力模型中"供应商议价能力"逻辑互补。

#### 2. 多阶段验证流程合理 ⭐⭐⭐⭐

趋势扫描 → 供应链拆解 → 瓶颈验证 → 候选映射 → 财务验证，逻辑链条完整，与真实投资研究的"宏观→产业→公司"流程一致。

#### 3. 退出信号 + 催化剂设计细致 ⭐⭐⭐⭐

覆盖技术替代、产能过剩、新进入者、需求放缓四大风险；催化剂要求每个候选至少有 1 个触发事件，解决"何时买"的问题。

#### 4. SerenityStrategy 财务验证提供安全边际 ⭐⭐⭐

毛利率/负债率/营收增速三道硬门槛，上下文感知（高评分放宽门槛），为 LLM 输出增加了可量化的验证层。

### ❌ 不合理之处（4/8 维）

#### 1. 数据源与目标严重不匹配 ⭐⭐

```
Phase 0 实际采集的数据：
  get_hot_stocks()       → 市场热度（短期价格信号）
  get_industry_ranking() → 行业涨跌排名（价格动量）
  get_cls_flash()        → 新闻快讯（媒体热点）
  get_north_bound_flow() → 资金流向（交易行为）

瓶颈分析真正需要的数据：
  × 全球供应链图谱（各环节供应商、份额）
  × 产能利用率历史数据
  × 资本开支/折旧比
  × 合同负债/在手订单
  × 技术专利分布与壁垒
```

Phase 0 实际上是"市场情绪扫描"，不是"供应链扫描"。用这些数据做产业链瓶颈分析，逻辑上是跳跃的。

#### 2. 所有评分是 LLM 主观判断，无法验证 ⭐

```json
{
  "supply_rigidity": { "score": 85, "detail": "..." },
  "composite_score": 85,
  "serenity_score": 75
}
```

这些数字来自 LLM 的"我觉得"，没有任何真实数据做锚定。对比股票分析 DAG 的 `compute_scoring` 工具有公式化计算逻辑，瓶颈掘金完全依赖 LLM 自由发挥。同一条产业链换一个模型跑，可能输出完全不同的数字。

#### 3. LLM 幻觉风险高 ⭐⭐

chain-decomposer prompt 要求输出：

```json
{
  "global_supplier_count": 3,
  "top3_market_share": 85,
  "expansion_cycle_months": 24
}
```

LLM 没有接入任何真实供应商数据库。对于成熟产业链（如存储芯片）这些数字可能来自训练数据，但对于新兴产业链（HBM4、固态电池、钙钛矿），训练数据中可能根本没有准确数据，LLM 会编造。

#### 4. 与股票分析 DAG 完全脱节 ⭐

PM 决策公式中的输入项：

| PM 使用的因子              | 来源                        | 是否包含 serenity 信息 |
| -------------------------- | --------------------------- | ---------------------- |
| compute_scoring.totalScore | 量化工具有 20+ 维度计算     | ❌ 不含                |
| consensus_score            | debate-convergence 辩论收敛 | ❌ 不含                |
| dqi_data_quality           | data-quality-inspector      | ❌ 不含                |
| risk_adjustment            | risk-convergence 收敛结论   | ❌ 不含                |
| catalyst_bonus             | catalyst-analyst            | ❌ 不含                |

**PM 公式中没有任何"瓶颈/供应链/Serenity"因子**。

---

## 六、综合评分

| 评估维度   |   评分   | 一句话结论                                   |
| ---------- | :------: | -------------------------------------------- |
| 方法论框架 | ⭐⭐⭐⭐ | 三力分析+多阶段验证，有产业经济学基础        |
| 数据基础   |   ⭐⭐   | 数据源与目标脱节，市场情绪数据做供应链分析   |
| 量化过程   |    ⭐    | 所有评分是 LLM 主观判断，无客观数据支撑      |
| 幻觉控制   |   ⭐⭐   | 具体数字不可靠，新兴产业链尤其严重           |
| 需求验证   |  ⭐⭐⭐  | search_news 是好设计，但执行不可控           |
| 退出机制   | ⭐⭐⭐⭐ | 覆盖四大风险，事件驱动逻辑清晰               |
| 财务验证   |  ⭐⭐⭐  | SerenityStrategy 硬门槛有效，但因子偏少      |
| 系统集成   |    ⭐    | 与股票分析 DAG 完全脱节，PM 无 serenity 权重 |

### 总评：2.5/5 ⭐⭐⭐ — "框架合理，执行薄弱"

瓶颈掘金是一个**方法论上站得住脚但执行上存在系统性缺陷**的分析工作流。

---

## 七、改进方向（按优先级排序）

### P0 — 打通系统隔阂（快速见效）

在 PM 公式中增加 `serenity_bonus` 因子，让股票分析 DAG 知道当前股票是经过供应链瓶颈验证的候选：

```
serenity_bonus = serenity_score / 20    // 20-85分 → +1 到 +4.25
```

同时降低 data-quality 门槛——对 serenity 候选股，dqi 门槛从 50 降到 30。

**修改文件**：

- `portfolio-manager.md`：增加 serenity_bonus 因子
- `stock_analysis_setup.rs`：给 data-quality 路径增加 serenity 降级处理

### P1 — 增强数据基础（中期）

Phase 0 增加真正的供应链数据源搜索。而不是只用市场情绪数据。

### P2 — 客观量化（长期）

将 LLM 的"我觉得 85 分"改为：

- 供应商集中度 → 基于行业数据库的真实 HHI 指数
- 技术壁垒 → 基于专利数量/年限/引用的客观指标
- 产能瓶颈 → 基于资本开支/折旧/稼动率的财务验证

### P3 — 分离风险判定框架

小盘低关注股不应与大盘蓝筹共用同一套风险模型。增设"小盘成长"风险判定路径。

---

## 八、关键文件索引

| 文件                       | 行号           | 内容                    |
| -------------------------- | -------------- | ----------------------- |
| `stock_analysis_setup.rs`  | 4917-5809      | Serenity 工作流模板定义 |
| `stock_workflow.rs`        | 3715-4128      | 运行时执行 + 持久化     |
| `trend-scanner.md`         | 全部           | 趋势扫描 prompt         |
| `chain-decomposer.md`      | 全部           | 产业链拆解 prompt       |
| `chokepoint-identifier.md` | 全部           | 瓶颈鉴定 prompt         |
| `candidate-mapper.md`      | 全部           | 候选映射 prompt         |
| `serenity.rs`              | 全部           | 财务验证策略            |
| `portfolio-manager.md`     | 85-153         | PM 决策公式             |
| `recommender/mod.rs`       | 42-83, 319-328 | 全局静态种子 + 注入     |
