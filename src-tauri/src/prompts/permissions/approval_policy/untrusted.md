Approval policy: untrusted (strictest).

- Only commands classified as safe run without asking.
- Everything else requires explicit user approval — including when no sandbox is
  active.

Keep commands minimal and single-purpose, and explain each one you ask to run.
