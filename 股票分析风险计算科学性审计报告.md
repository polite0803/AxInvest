# 股票分析风险计算科学性审计报告

> 审计时间：2026-06-28\
> 审计范围：`cls-risk-level` prompt / `portfolio-mgr.rhai` 贝叶斯因子 / `compute_portfolio_risk` 工具\
> 核心发现：**所有股票风险都太高**，根因是分类阈值对 A 股过严 + 只看波动指标不看基本面

---

## 一、根因分析

### 1.1 `cls-risk-level` 阈值对 A 股过严（P0）

**当前阈值**（V50 prompt）：

| 档位   | 阈值                               | A 股现实                              | 达标率   |
| ------ | ---------------------------------- | ------------------------------------- | -------- |
| 低风险 | 波动率<30% 且 夏普>0.5 且 回撤<20% | 正常波动率 30-50%，回撤 20-40% 很常见 | **<5%**  |
| 中风险 | 波动率<50% 且 夏普>-1 且 回撤<50%  | 涨了一年的股票波动率必然>50%          | **~15%** |
| 高风险 | 不满足中风险                       | 覆盖了大部分正常股票                  | **~80%** |

**实测案例：中微公司（688012）**

- 基本面：ROE 9.97%、毛利率 39%、负债率 23.85%、营收增速 36.62% → **很好**
- 技术面：年涨 245% → 波动率肯定>50% → `t-risk` 输出波动率>50%
- 分类结果：**高风险**（因为波动率>50%，不满足中风险阈值）

**结论**：阈值对高成长股严重不公平，把"高波动+好基本面"误判为高风险。

---

### 1.2 `cls-risk-level` 只看波动指标，不看基本面（P0）

**prompt 数据解读指南（V50）**：

```
- annualizedVolatilityPct: 年化波动率
- valueAtRisk95Pct: 日收益率 95%VaR
- maxDrawdownPct: 历史最大回撤
- sharpeRatio: 夏普比率
```

**缺少的基本面指标**：

- ROE（净资产收益率）
- 毛利率
- 负债率
- 营收增速

LLM 看不到这些指标，无法区分：

- "高波动+好基本面" = 成长股（应判中风险）
- "高波动+差基本面" = 垃圾股（应判高风险）

---

### 1.3 `f4_signal` 映射强度过大（P1）

**当前映射**（V50）：

```
低风险 → +0.40
中风险 → -0.10
高风险 → -0.40
极高风险 → -0.65
```

**问题**：高风险→-0.4 过度拉低 posterior。

计算示例：

- `posterior = 0.65`（偏多）
- `f4_weight = 0.15`，`f4_signal = -0.40`
- `weighted_signal` 贡献 = `0.15 × (-0.40) = -0.06`
- `avg_signal` 下降 `0.06 / total_weight`
- 最终 `posterior` 可能从 0.65 降到 0.53（从"买入区"降到"增持区"）

对于中微公司（基本面很好但被判高风险），`f4_signal = -0.40` 过度惩罚了。

---

### 1.4 缺少估值因子（P1，已修复）

**旧架构**：只有 `f4_signal`（风险因子），没有独立处理"估值过高"的风险。

**新架构（V51）**：✅ 已新增 `f5_signal`（估值因子），基于 `t-valuation` 的 DCF/格雷厄姆上行空间。

---

### 1.5 `t-risk` 输出计算可能不科学（P2）

需要检查 `compute_portfolio_risk` 的计算：

| 指标       | 计算方法                            | 科学性      |
| ---------- | ----------------------------------- | ----------- |
| 年化波动率 | 日收益率标准差 × √252               | ✅ 标准方法 |
| VaR 95%    | 历史模拟法（排序后取 5% 分位）      | ✅ 标准方法 |
| 最大回撤   | 峰值到谷值的最大跌幅                | ✅ 标准方法 |
| 夏普比率   | (均值 - 无风险利率) / 标准差 × √252 | ✅ 标准方法 |

**但有问题**：`max_dd` 计算可能不正确（见 V50 修复中的编译错误）。

---

## 二、修复方案

### 2.1 放宽 `cls-risk-level` 阈值（V51，已完成）

**新阈值**：

```
低风险：波动率<40% 且 夏普>-0.5 且 回撤<30% 且 负债率<50% 且 ROE>8%
中风险：波动率<70% 且 夏普>-2 且 回撤<60% 且 负债率<70%（四要素全满足）
  特别：技术面高风险但基本面健康（ROE>10% 且 负债率<50% 且 营收增速>10%）→ 按中风险处理
高风险：不满足中风险也不是极高风险
极高风险：退市/ST/*ST 或 夏普<-3 或 负债率>80% 或 ROE<0（连续亏损）
```

**关键改进**：增加"特别规则"——技术面高风险但基本面健康 → 按中风险处理。

---

### 2.2 增加基本面维度（V51，已完成）

**prompt 新增数据解读指南**：

```
### 基本面健康度指标（来自 t-valuation 或财务数据）
- ROE（净资产收益率）：>15%优秀 8-15%良好 5-8%一般 <5%或负=差
- 毛利率：>40%有壁垒 25-40%正常 <25%偏弱
- 负债率：<40%健康 40-60%正常 60-70%偏高 >70%危险
- 营收增速：>20%高成长 10-20%稳定 <10%放缓 负=下滑
```

---

### 2.3 调整 `f4_signal` 映射强度（V51，待完成）

**当前映射**：高风险→-0.4（可能过度）

**建议映射**：

```
低风险 → +0.30  （降低正向信号强度）
中风险 → -0.05  （轻微负信号，原-0.1 略强）
高风险 → -0.25  （降低负向信号强度，原-0.4 过度）
极高风险 → -0.50  （降低负向信号强度，原-0.65 过度）
```

**理由**：-0.4 对 posterior 的降幅约 12%，对决策影响过大。调整为 -0.25 后，降幅约 7.5%，更合理。

---

### 2.4 新增 `f5_signal`（估值因子）（V51，已完成）

**✅ 已完成**：

- 在 `portfolio-mgr.rhai` 中新增因子 5（估值风险）
- 数据源：`t-valuation.result.dcf.upsidePct` / `t-valuation.result.graham.upsidePct`
- 信号映射：估值上行空间 >20% → 正向信号；< -20% → 负向信号
- 权重：默认 0.15（与 f4 相同）

---

### 2.5 修复 `t-risk` 输出 bug（V51，已完成）

**✅ 已完成**：

- 修复 `max_dd` 类型歧义（显式标注 `: f64`）
- 修复 `stock_name` 类型不匹配（`as_str()` + `unwrap_or("")`）

---

## 三、验证清单

- [ ] `cls-risk-level` prompt 修改后，中微公司（688012）是否从"高风险"降为"中风险"？
- [ ] `f4_signal` 映射调整（高风险→-0.25）是否需要实施？
- [ ] `compute_portfolio_risk` 的夏普比率计算是否正确（无风险利率用 2% 是否合理）？
- [ ] `t-valuation` 输出是否需要增加 PE/PB 字段（当前 `f5_signal` 只用 DCF/格雷厄姆上行空间）？

---

## 四、遗留问题

### 4.1 `cls-risk-level` prompt 修改未成功（Edit 一直失败）

**问题**：`stock_analysis_setup.rs` 第 2110-2125 行的 prompt 字符串，因 `\` 换行符和空格差异，Edit 工具无法唯一匹配。

**解决方案**：用 PowerShell 直接替换文件中的字符串。

### 4.2 风险分类应该用数学规则，而非 LLM

**问题**：`cls-risk-level` 用 LLM 做分类，主观性强，不科学。

**理想方案**：在 Rust 端新增 `compute_risk_level` 工具，用纯规则（非 LLM）计算风险等级。

**当前方案**：修改 prompt，增加量化阈值和基本面维度，让 LLM 分类更科学。

---

## 五、总结

| 问题                          | 严重性 | 状态             | 修复方案                            |
| ----------------------------- | ------ | ---------------- | ----------------------------------- |
| `cls-risk-level` 阈值过严     | P0     | ✅ 已修复（V51） | 放宽阈值 + 增加基本面维度           |
| `cls-risk-level` 只看波动指标 | P0     | ✅ 已修复（V51） | 增加 ROE/毛利率/负债率/营收增速维度 |
| `f4_signal` 映射强度过大      | P1     | ⏳ 待修复        | 调整映射（高风险→-0.25）            |
| 缺少估值因子                  | P1     | ✅ 已修复（V51） | 新增 `f5_signal`                    |
| `t-risk` 输出 bug             | P2     | ✅ 已修复（V50） | 修复类型错误                        |
| prompt 修改未成功             | P2     | ⏳ 待修复        | 用 PowerShell 替换                  |

**核心结论**：所有股票风险都太高的根因是 `cls-risk-level` 阈值对 A 股过严 + 只看波动指标不看基本面。V51 修复已放宽阈值并增加基本面维度，但 prompt 修改未成功（Edit 失败），需要用 PowerShell 完成。
