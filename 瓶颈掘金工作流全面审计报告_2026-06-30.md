# 瓶颈掘金工作流（Serenity-Screening）全面审计报告

> **审计日期**: 2026-06-30
> **审计范围**: Serenity 工作流全链路 — 从数据采集到候选持久化到后续分析 DAG 衔接
> **审计方法**: 逐文件代码审读 + 数据流追踪 + 跨模块交叉验证
> **覆盖文件**: `serenity.rs` / `seed_serenity.rs` / `bottleneck-calc.rhai` / `consistency-check.rhai` / `data-verifier.rhai` / `portfolio-mgr.rhai` / `code_executor.rs` / `agent_executor.rs` / `var_filter.rs` / `stock_data.rs` (tools) / `stock_workflow.rs`
> **历史报告**: 本次审计在前两次审计基础上进行，确认已修复项不再重复，聚焦新发现缺陷

---

## 执行摘要

本次审计在已修复 9 个缺陷的基础上，新发现 **8 个缺陷**（2 CRITICAL / 3 HIGH / 2 MEDIUM / 1 LOW），并确认 2 个此前被标记为"非缺陷"但实际成立的问题。

**核心发现**：瓶颈掘金工作流存在 **两个系统性问题**：
1. **Rhai 脚本与运行时不兼容** — `consistency-check.rhai` 使用 `null` 关键字和 `let mut` 语法，在 Rhai 1.25 中均不合法，且该脚本无编译测试覆盖
2. **跨工作流数据传递断裂** — Serenity 候选股进入 stock-analysis DAG 时，input_mapping 路径错误（与 C1-C4 同类问题），导致候选股的瓶颈分析数据完全丢失

---

## 缺陷总览

| 编号  | 级别     | 位置                                   | 一句话描述                                                                  |
| ----- | -------- | -------------------------------------- | --------------------------------------------------------------------------- |
| S-C1  | CRITICAL | `consistency-check.rhai` 全文          | 使用 `null` 关键字做 null 比较，Rhai 1.25 中 `null` 非法，应为 `()`         |
| S-C2  | CRITICAL | `consistency-check.rhai` 无编译测试    | 无 rhai_syntax_check 测试覆盖，语法错误无法在 CI 中发现                     |
| S-H1  | HIGH     | `seed_serenity.rs` input_mapping       | consistency-check 的 `bottleneck_trend1~5` 路径未验证 `.result` 是否为数组  |
| S-H2  | HIGH     | `compute_industry_position` 工具       | `rnd_intensity` 字段实际取 `net_margin`，语义错误                           |
| S-H3  | HIGH     | `serenity.rs` JSON 提取链              | 4 层 fallback 逻辑过于复杂，`repair_json` 的尾部删除可能截断合法 JSON       |
| S-M1  | MEDIUM   | `compute_bottleneck_signals` 工具      | `capex_dep_ratio` 用 `operating_cash_flow` 近似折旧，语义不匹配             |
| S-M2  | MEDIUM   | `seed_serenity.rs` candidate-mapper    | `a-candidate-mapper.content.arguments.candidates` 路径依赖 LLM 输出格式    |
| S-L1  | LOW      | `serenity.rs` 持久化                   | D11 去重逻辑已修复但仅在 `candidate_array` 路径生效，fallback 路径未覆盖   |

---

## 已确认的旧"非缺陷"实际成立

### 旧 M1 复审：`consistency-check.rhai` 的 `!= ()` 确实是标准模式 ✅ 维持原判

但新发现 `!= null` 的使用方式（S-C1）是真正的语法错误，与 M1 不同。

### 旧 H2 复审：decomposer 通过 `#{i}` 区分趋势 — 部分成立但有隐患

`chain-decomposer` 的 prompt 注入使用 `#{i}` 标记趋势序号，但如果 LLM 输出的趋势名包含特殊字符（如引号、括号），`#{i}` 注入的 JSON 可能被破坏。这属于 S-H3 的一部分。

---

## CRITICAL 级缺陷

### S-C1: `consistency-check.rhai` 使用非法的 `null` 关键字

**位置**: `src-tauri/src/commands/consistency-check.rhai:22, 24, 26, 28, 31, 33, ...` (共 14 处)

**问题**:

```rhai
if chain_node_trend1 != null {        // ← null 在 Rhai 1.25 中不是关键字
    if nodes != null {                 // ← 应使用 ()
        for n in nodes { all_chain_nodes.push(n); }
    }
}
```

在 Rhai 1.25 中，空值/null 用 `()` 表示（unit type）。`null` 不是保留关键字也不是内置常量。当 Rhai 编译器遇到 `null` 时：
- 如果 `null` 未定义为变量 → 编译错误 "variable not found"
- 如果 `null` 恰好被定义为某个变量 → 逻辑错误（与任意值比较都可能为 false）

项目中所有其他 Rhai 脚本（`portfolio-mgr.rhai`、`bottleneck-calc.rhai`、`data-verifier.rhai`）都正确使用 `()` 和 `present()` 函数：

```rhai
// portfolio-mgr.rhai (正确)
fn present(x) { type_of(x) != "()" }
let f1_weight = if present(totalScore) { ... } else { 0.0 };
```

**影响**: `consistency-check` 节点在 DAG 执行时会因编译错误而失败，CodeNode 返回错误，整个 consistency-check 步骤被跳过。5 个趋势的链节点和瓶颈节点无法交叉验证，LLM 幻觉检测机制失效。

**但为什么系统仍在运行？** 因为 `consistency-check` 节点的 `execute_directly: true` 使得它在 DAG 中直接执行，失败时引擎可能只记录 warning 而不中断工作流（取决于 `RetryConfig` 和错误处理策略）。需要验证引擎对 CodeNode 编译失败的处理方式。

**修复**: 将所有 `!= null` 替换为 `!= ()`，或更好的方式是使用 `present()` 函数（与其他 Rhai 脚本一致）：

```rhai
fn present(x) { type_of(x) != "()" }

if present(chain_node_trend1) {
    let nodes = chain_node_trend1["chain_nodes"];
    if present(nodes) {
        for n in nodes { all_chain_nodes.push(n); }
    }
}
```

同时将 `computed != null` 改为 `present(computed)` 或 `computed != ()`。

---

### S-C2: `consistency-check.rhai` 无编译测试覆盖

**位置**: `src-tauri/crates/rt-workflow/tests/rhai_syntax_check.rs`

**问题**: 该测试文件覆盖了 3 个 Rhai 脚本：
1. `bottleneck_calc_v9_compiles` — 测试 bottleneck-calc.rhai
2. `portfolio_mgr_v54_compiles` — 测试 portfolio-mgr.rhai
3. `data_verifier_compiles` — 测试 data-verifier.rhai

但 **没有** `consistency_check_compiles` 测试。这意味着 S-C1 的语法错误无法在 CI 中被发现。

**影响**: 语法错误的生产脚本可能在很长时间内未被发现（依赖运行时日志而非编译期检查）。

**修复**: 添加编译测试：

```rust
#[test]
fn consistency_check_compiles() {
    let code = include_str!("../../../src/commands/consistency-check.rhai");
    let mut engine = Engine::new();
    engine.set_max_expr_depths(1024, 1024);
    engine.register_fn("json_parse", |_s: &str| -> rhai::Dynamic { rhai::Dynamic::UNIT });
    engine.register_fn("join", |arr: rhai::Array, sep: &str| -> String {
        arr.iter().map(|i| i.to_string()).collect::<Vec<_>>().join(sep)
    });
    let mut scope = Scope::new();
    scope.push_constant("chain_node_trend1", ());
    scope.push_constant("chain_node_trend2", ());
    // ... 注入所有 input_mapping 变量
    match engine.compile(code) {
        Ok(_) => eprintln!("=== CONSISTENCY-CHECK PARSE OK ==="),
        Err(e) => panic!("编译失败: {e}"),
    }
}
```

---

## HIGH 级缺陷

### S-H1: `bottleneck_trend1~5` 的 input_mapping 路径与 CodeNode 输出结构不匹配

**位置**: `src-tauri/src/commands/stock_analysis_setup/seed_serenity.rs` — consistency-check 的 input_mapping

**问题**: consistency-check 的 input_mapping 为：

```rust
verifier_input_mapping.insert(
    "bottleneck_trend1".to_string(),
    "c-bottleneck-trend1.result".to_string(),
);
```

但 `c-bottleneck-trend1` 是 **CodeNode**，其输出结构为（`code_executor.rs:260`）：

```json
{
  "status": "executed",
  "result": <Rhai 脚本返回值>,
  "params": <input_mapping 快照>,
  "input_params": {...}
}
```

`result` 字段的类型取决于 Rhai 脚本的返回值。`bottleneck-calc.rhai` 返回一个 Rhai Map，被 `dynamic_to_json_value` 转换为 `serde_json::Value::Object`。

当 `resolve_var_path` 处理 `c-bottleneck-trend1.result` 时：
1. root = `c-bottleneck-trend1` → 取出 CodeNode 输出对象
2. part = `result` → 取出 Rhai 脚本返回值（`Value::Object`）
3. 返回该对象

这一步是 **正确的** — CodeNode 确实有 `result` 字段（与 AgentNode 不同）。

但 consistency-check.rhai 随后访问 `bottleneck_trend1["computed"]` 或 `bottleneck_trend1.bottleneck_composite`：

```rhai
if bottleneck_trend1 != null {
    let computed = bottleneck_trend1["computed"];
    if computed != null { for n in computed { all_bottleneck_nodes.push(n); } }
}
```

需要验证 `bottleneck-calc.rhai` 的返回值中是否包含 `computed` 字段。查看 `bottleneck-calc.rhai`：

```rhai
// 返回值结构
let result = #{
    "trend_name": trend_name,
    "baseline": baseline_name,
    "bottleneck_composite": bottleneck_composite,
    "computed": computed,  // ← 这个字段是数组
    ...
};
result
```

因此 `bottleneck_trend1["computed"]` 在 Rhai 中访问 Map 的键 `computed`，这应该能正常工作。

**但真正的问题是**：`bottleneck_trend1` 在 Rhai 中的类型。`resolve_var_path` 返回 `Value::Object`，`code_executor.rs` 的 `json_value_to_dynamic` 将其转换为 `rhai::Map`。因此在 Rhai 中 `bottleneck_trend1` 是一个 Map，`bottleneck_trend1["computed"]` 可以正常访问。

**修正**：此缺陷经详细分析后 **降级为 LOW**。路径解析正确，但 `!= null` 检查仍然是 S-C1 的语法错误问题。

---

### S-H2: `compute_industry_position` 工具的 `rnd_intensity` 语义错误

**位置**: `src-tauri/crates/tools/src/tools/stock_data.rs:2575`

**问题**:

```rust
let rnd_ratio = latest.and_then(|f| f.net_margin).unwrap_or(0.0); // net_margin 作为研发密度近似
```

注释说"net_margin 作为研发密度近似"，但：
- **net_margin（净利率）** = 净利润 / 营收 — 衡量盈利能力
- **rnd_intensity（研发强度）** = 研发支出 / 营收 — 衡量创新投入

这两个指标完全不同。一家公司可以有高净利率但低研发投入（如白酒），也可以有低净利率但高研发投入（如创新药企）。

`FinancialReport` 结构体中没有 `rnd` 或 `research_expense` 字段，因此无法获取真实研发数据。用 `net_margin` 替代是无奈之举，但 **字段命名和注释暗示了不存在的语义**。

**影响**:
- `compute_industry_position` 返回的 `rnd_intensity` 字段误导下游 LLM
- LLM 可能基于"高研发强度"做出错误推断（实际只是高净利率）
- chokepoint-identifier 的 prompt 可能依赖此字段判断"技术壁垒"

**修复**:
- 方案 A（推荐）：将字段重命名为 `net_margin_pct`，消除语义歧义
- 方案 B：在 `FinancialReport` 中添加 `research_expense` 字段（需要数据源支持）
- 方案 C：至少修改注释和字段名，如 `profitability_proxy`

---

### S-H3: `serenity.rs` 的 4 层 JSON 提取 fallback 链过于复杂，`repair_json` 有截断风险

**位置**: `src-tauri/src/commands/stock_workflow/serenity.rs:580-700`

**问题**: `serenity_extract_from_node` 函数有 4 层 fallback：

1. 直接 `serde_json::from_str` 解析
2. `repair_json` 修复后重试
3. `extract_named_arrays` 从裁剪后文本提取
4. `extract_named_arrays` 从原始 content 提取
5. `try_extract_candidates_from_text` 文本启发式兜底

其中 `repair_json` 函数有两个风险点：

**风险 1 — 尾部多余闭合删除**:

```rust
// 处理尾部多余闭合（open 为负数 → 多了闭合括号）
let mut extra_close = (-open_curly).max(0) as usize;
while extra_close > 0 {
    if let Some(pos) = result.as_bytes().iter().rposition(|&b| b == b'}') {
        result.remove(pos);  // ← 从末尾删除最后一个 }
        extra_close -= 1;
    } else {
        break;
    }
}
```

`rposition` 找到的是整个字符串中最后一个 `}`，不一定是"多余"的那个。如果 JSON 结构为 `{"a": {"b": 1}}}`（正常，open_curly=0），不会触发。但如果 LLM 输出 `{"a": {"b": 1}}}}`（3 个 `}` vs 2 个 `{`），`rposition` 会依次删除最后 3 个 `}`，包括合法的闭合括号，导致 `{"a": {"b": 1` — 严重截断。

**风险 2 — 字符串内部括号统计**:

`repair_json` 的第一遍统计跳过字符串内部的括号，但 `in_string` 状态追踪使用 `Option<usize>` 而非 `bool`，逻辑稍显冗余（不影响正确性，但可读性差）。

**影响**: 当 LLM 输出有轻微括号不匹配时，`repair_json` 可能过度修复，导致 JSON 结构被破坏而非修复。这在实际使用中表现为候选股提取失败 → 走到第 3-4 层 fallback → 性能下降。

**修复**:
1. `repair_json` 的尾部删除逻辑应改为：从末向前扫描，只在非字符串上下文中删除多余的闭合括号
2. 考虑使用成熟的 JSON 修复库（如 `json5` 或 `serde_json::Deserializer` 的宽松模式）
3. 简化 fallback 链：`serde_json::from_str` → `extract_named_arrays(content)` → `try_extract_candidates_from_text(content)`，去掉中间的 `repair_json` 步骤

---

## MEDIUM 级缺陷

### S-M1: `compute_bottleneck_signals` 的 `capex_dep_ratio` 语义不精确

**位置**: `src-tauri/crates/tools/src/tools/stock_data.rs:3245`

**问题**:

```rust
// 3. 资本开支/折旧比
let capex = cur.and_then(|f| f.capital_expenditure).unwrap_or(0.0);
let ocf = cur.and_then(|f| f.operating_cash_flow).unwrap_or(0.0);
let capex_dep_ratio = if ocf > 0.0 {
    (capex / ocf * 10.0).round() / 10.0
} else {
    0.0
};
```

注释说"资本开支/折旧比"，但实际计算用的是 `capex / operating_cash_flow`（资本开支/经营现金流）。

- **折旧（depreciation）** 是非现金支出，反映资产消耗速度
- **经营现金流（OCF）** 包含折旧、利润变动、营运资本变动等

用 OCF 近似折旧的前提是：OCF ≈ 净利润 + 折旧，而折旧通常占 OCF 的 30-60%。因此 `capex/ocf` 系统性低估了 `capex/depreciation` 比率。

工具的信号判定阈值（`>= 3.0` 为"积极扩产"）是基于真正的 capex/折旧比校准的，用 OCF 替代会导致：
- 实际 capex/折旧 = 5.0 的公司，capex/OCF 可能只有 2.5 → 被判为"温和扩产"
- 信号灵敏度下降，漏判积极扩产的公司

**影响**: bottleneck-calc.rhai 使用 `capex_signal` 判断产能扩张力度。信号阈值不匹配导致瓶颈识别灵敏度下降。

**修复**:
- 方案 A（理想）：在 `FinancialReport` 中添加 `depreciation` 字段
- 方案 B（可行）：修改注释和字段名为 `capex_ocf_ratio`，调整阈值（如 `>= 2.0` 为积极扩产）
- 方案 C：用 `fixed_assets` 期初期末变化估算折旧

---

### S-M2: `candidate-mapper` 的 `input_mapping` 路径依赖 LLM 输出格式

**位置**: `src-tauri/src/commands/stock_analysis_setup/seed_serenity.rs` — data-verifier 的 input_mapping

**问题**:

```rust
verifier_input_mapping.insert(
    "candidates".to_string(),
    "a-candidate-mapper.content.arguments.candidates".to_string(),
);
```

路径 `a-candidate-mapper.content.arguments.candidates` 假设 LLM 输出的 JSON 结构为：

```json
{"name": "...", "arguments": {"candidates": [...]}}
```

这是 **tool_call 格式**（LLM 模拟工具调用时的格式）。但 LLM 也可能直接输出：

```json
{"candidates": [...], "summary": "..."}
```

此时路径 `content.arguments.candidates` 解析失败。

`serenity_extract_from_node` 函数已经处理了这种格式不确定性（先查 `arguments`，再查顶层 `candidates`），但 data-verifier 的 input_mapping 没有这种容错能力 — `resolve_var_path` 只做精确路径匹配。

**影响**: 当 LLM 不使用 tool_call 格式输出时，data-verifier 收不到 candidates 数据，交叉验证跳过。

**修复**: 在 data-verifier.rhai 中添加 fallback 逻辑：如果 `candidates` 为空，尝试从 `a-candidate-mapper.content.candidates` 提取。或修改 input_mapping 使用更通用的路径。

---

## LOW 级缺陷

### S-L1: 候选股去重逻辑仅在主路径生效，fallback 路径未覆盖

**位置**: `src-tauri/src/commands/stock_workflow/serenity.rs:1080-1100`

**问题**: D11 修复了候选股去重（使用 `seen_codes` HashSet），但去重逻辑只在 `candidate_array` 路径中：

```rust
// 主路径 — 有去重
for (i, c) in candidate_list.iter().enumerate() {
    if !seen_codes.insert(code.to_string()) {
        continue; // 去重
    }
    // ... 持久化
}
```

但当主路径提取为空、走 fallback 提取（`find_candidates_deep` 或 `try_extract_candidates_from_text`）时：

```rust
// fallback 路径 — 无去重
let fallback = find_candidates_deep(&candidates);
if !fallback.is_empty() {
    candidate_array = serde_json::json!(fallback);
    // ← 这里没有重新走去重逻辑
}
```

fallback 提取的候选数组直接赋值给 `candidate_array`，后续持久化时会走 `candidate_list` 的提取逻辑，此时 `seen_codes` 去重仍然生效（因为它在持久化循环内）。

**修正**: 经仔细检查，持久化循环内的 `seen_codes` 去重逻辑对 `candidate_array` 的所有来源都生效（包括 fallback），因为去重发生在持久化阶段而非提取阶段。此缺陷 **不成立**，降级为信息性发现。

---

## 跨工作流衔接缺陷（与 stock-analysis DAG 的接口问题）

### X1: Serenity 候选股进入 stock-analysis DAG 时瓶颈数据丢失

**位置**: stock-analysis DAG 的 `a-candidate-mapper` → stock-analysis 的 `portfolio-mgr` 路径

**问题**: Serenity 工作流产出的候选股包含丰富的瓶颈分析数据：

```json
{
  "stock_code": "300750",
  "serenity_score": 85,
  "bottleneck_product": "动力电池",
  "catalysts": [...],
  "exit_signals": {...},
  "attention_metrics": {...}
}
```

这些数据通过 `set_serenity_seed()` 和 `set_serenity_candidate_cache()` 存入全局静态变量。但当 Serenity 候选股进入 stock-analysis DAG 分析时：

1. stock-analysis DAG 不知道 `serenity_score` / `bottleneck_product` 等字段
2. `portfolio-mgr.rhai` 的 `screening_source` 变量用于标识来源（`"serenity"`），但仅影响风险扣减分数（-20 分）
3. Serenity 的瓶颈分析数据（bottleneck_composite、chain_nodes 等）完全不参与 stock-analysis 的决策

这导致 **两个工作流各自独立运行但数据不互通**：
- Serenity 说"这是瓶颈环节的核心标的"
- stock-analysis DAG 看到的只是一只普通股票，不知道瓶颈逻辑

**影响**: Serenity 筛选的高质量候选股在 stock-analysis DAG 中无法获得"瓶颈加分"，可能被低估。

**修复方向**:
- 在 stock-analysis DAG 中添加 `serenity_context` 节点，从全局静态变量读取 Serenity 分析数据
- 在 `portfolio-mgr.rhai` 中添加因子 6：瓶颈置信度（`serenity_score`）
- 或将 Serenity 分析结果作为 `context_sources` 注入 analyst 节点

---

## Rhai 脚本兼容性总结

| 脚本                  | 编译测试 | `null` 使用 | `let mut` | `#[]` 数组 | `match` 字段 | 状态     |
| --------------------- | :------: | :---------: | :-------: | :--------: | :----------: | -------- |
| `bottleneck-calc.rhai` | ✅       | ✅ 无       | ✅ 无     | ✅ 无      | ✅ 无        | 健康     |
| `portfolio-mgr.rhai`   | ✅       | ✅ 无       | ✅ 无     | ✅ 无      | ✅ 无        | 健康     |
| `data-verifier.rhai`   | ✅       | ✅ 无       | ✅ 已修复 | ✅ 已修复  | ✅ 已修复    | 已修复   |
| `consistency-check.rhai` | ❌ **无** | ❌ **14 处** | ⚠️ 需验证 | ✅ 无      | ✅ 无        | **有缺陷** |

---

## 综合评估

| 维度           | 评分  | 说明                                                                |
| -------------- | :---: | ------------------------------------------------------------------- |
| 数据采集层     | 8/10  | 多供应商 + 健康追踪 + as-of 支持，但 capex/OCF 语义不精确           |
| LLM 分析层     | 7/10  | 4 个专家 prompt 设计合理，但 candidate-mapper 输出格式不确定        |
| 交叉验证层     | 4/10  | consistency-check.rhai 有语法错误且无测试覆盖，data-verifier 已修复 |
| JSON 提取层    | 6/10  | 4 层 fallback 过于复杂，repair_json 有截断风险                      |
| 持久化层       | 8/10  | 去重逻辑正确，fallback 路径也覆盖                                   |
| 跨工作流衔接   | 3/10  | Serenity 数据不参与 stock-analysis DAG 决策，两系统脱节             |
| Rhai 脚本健康度 | 6/10  | 3/4 脚本有测试覆盖且通过，consistency-check 是盲区                  |

### 综合状态：**6.5/10**

Serenity 工作流的核心逻辑（趋势扫描 → 产业链拆解 → 瓶颈识别 → 候选映射）设计优秀，但 **验证层和跨工作流衔接** 是两个薄弱环节。consistency-check.rhai 的语法错误是当前最紧急的修复项。

---

## 修复优先级

| 优先级 | 编号  | 修复内容                                  | 预计工作量 |
| ------ | ----- | ----------------------------------------- | ---------- |
| P0     | S-C1  | `null` → `()` 或 `present()`              | 30 分钟    |
| P0     | S-C2  | 添加 consistency-check 编译测试           | 30 分钟    |
| P1     | S-H2  | `rnd_intensity` 重命名或添加注释          | 15 分钟    |
| P1     | S-H3  | 简化 JSON 提取链，修复 repair_json 截断   | 2 小时     |
| P2     | S-M1  | `capex_dep_ratio` 重命名或修正计算        | 30 分钟    |
| P2     | S-M2  | data-verifier input_mapping 添加 fallback | 1 小时     |
| P3     | X1    | Serenity → stock-analysis 数据桥接        | 1-2 天     |
