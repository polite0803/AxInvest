from setuptools import setup, find_packages

setup(
    name="axagent-sdk",
    version="1.3.5",
    description="AxAgent Python SDK — ACP 协议客户端",
    author="AxAgent Team",
    packages=find_packages(),
    python_requires=">=3.8",
)
